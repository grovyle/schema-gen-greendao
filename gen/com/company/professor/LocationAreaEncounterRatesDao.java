package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.LocationAreaEncounterRates;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "location_area_encounter_rates".
*/
public class LocationAreaEncounterRatesDao extends AbstractDao<LocationAreaEncounterRates, Long> {

    public static final String TABLENAME = "location_area_encounter_rates";

    /**
     * Properties of entity LocationAreaEncounterRates.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property LocationAreaId = new Property(0, long.class, "LocationAreaId", true, "location_area_id");
        public final static Property EncounterMethodId = new Property(1, long.class, "EncounterMethodId", false, "encounter_method_id");
        public final static Property VersionId = new Property(2, long.class, "VersionId", false, "version_id");
        public final static Property Rate = new Property(3, Long.class, "Rate", false, "rate");
    };

    private DaoSession daoSession;


    public LocationAreaEncounterRatesDao(DaoConfig config) {
        super(config);
    }
    
    public LocationAreaEncounterRatesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"location_area_encounter_rates\" (" + //
                "\"location_area_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: LocationAreaId
                "\"encounter_method_id\" INTEGER NOT NULL ," + // 1: EncounterMethodId
                "\"version_id\" INTEGER NOT NULL ," + // 2: VersionId
                "\"rate\" INTEGER);"); // 3: Rate
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_location_area_encounter_rates_location_area_id_encounter_method_id_version_id ON location_area_encounter_rates" +
                " (\"location_area_id\",\"encounter_method_id\",\"version_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"location_area_encounter_rates\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, LocationAreaEncounterRates entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getLocationAreaId());
        stmt.bindLong(2, entity.getEncounterMethodId());
        stmt.bindLong(3, entity.getVersionId());
 
        Long Rate = entity.getRate();
        if (Rate != null) {
            stmt.bindLong(4, Rate);
        }
    }

    @Override
    protected void attachEntity(LocationAreaEncounterRates entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public LocationAreaEncounterRates readEntity(Cursor cursor, int offset) {
        LocationAreaEncounterRates entity = new LocationAreaEncounterRates( //
            cursor.getLong(offset + 0), // LocationAreaId
            cursor.getLong(offset + 1), // EncounterMethodId
            cursor.getLong(offset + 2), // VersionId
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // Rate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, LocationAreaEncounterRates entity, int offset) {
        entity.setLocationAreaId(cursor.getLong(offset + 0));
        entity.setEncounterMethodId(cursor.getLong(offset + 1));
        entity.setVersionId(cursor.getLong(offset + 2));
        entity.setRate(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(LocationAreaEncounterRates entity, long rowId) {
        entity.setLocationAreaId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(LocationAreaEncounterRates entity) {
        if(entity != null) {
            return entity.getLocationAreaId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getLocationAreasDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getEncounterMethodsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getVersionsDao().getAllColumns());
            builder.append(" FROM location_area_encounter_rates T");
            builder.append(" LEFT JOIN location_areas T0 ON T.\"location_area_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN encounter_methods T1 ON T.\"encounter_method_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN versions T2 ON T.\"version_id\"=T2.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected LocationAreaEncounterRates loadCurrentDeep(Cursor cursor, boolean lock) {
        LocationAreaEncounterRates entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        LocationAreas LocationAreas = loadCurrentOther(daoSession.getLocationAreasDao(), cursor, offset);
         if(LocationAreas != null) {
            entity.setLocationAreas(LocationAreas);
        }
        offset += daoSession.getLocationAreasDao().getAllColumns().length;

        EncounterMethods EncounterMethods = loadCurrentOther(daoSession.getEncounterMethodsDao(), cursor, offset);
         if(EncounterMethods != null) {
            entity.setEncounterMethods(EncounterMethods);
        }
        offset += daoSession.getEncounterMethodsDao().getAllColumns().length;

        Versions Versions = loadCurrentOther(daoSession.getVersionsDao(), cursor, offset);
         if(Versions != null) {
            entity.setVersions(Versions);
        }

        return entity;    
    }

    public LocationAreaEncounterRates loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<LocationAreaEncounterRates> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<LocationAreaEncounterRates> list = new ArrayList<LocationAreaEncounterRates>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<LocationAreaEncounterRates> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<LocationAreaEncounterRates> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
