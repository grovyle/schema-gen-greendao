package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ContestCombos;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "contest_combos".
*/
public class ContestCombosDao extends AbstractDao<ContestCombos, Long> {

    public static final String TABLENAME = "contest_combos";

    /**
     * Properties of entity ContestCombos.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property FirstMoveId = new Property(0, long.class, "FirstMoveId", true, "first_move_id");
        public final static Property SecondMoveId = new Property(1, long.class, "SecondMoveId", false, "second_move_id");
    };

    private DaoSession daoSession;


    public ContestCombosDao(DaoConfig config) {
        super(config);
    }
    
    public ContestCombosDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"contest_combos\" (" + //
                "\"first_move_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: FirstMoveId
                "\"second_move_id\" INTEGER NOT NULL );"); // 1: SecondMoveId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_contest_combos_first_move_id_second_move_id ON contest_combos" +
                " (\"first_move_id\",\"second_move_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"contest_combos\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ContestCombos entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getFirstMoveId());
        stmt.bindLong(2, entity.getSecondMoveId());
    }

    @Override
    protected void attachEntity(ContestCombos entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ContestCombos readEntity(Cursor cursor, int offset) {
        ContestCombos entity = new ContestCombos( //
            cursor.getLong(offset + 0), // FirstMoveId
            cursor.getLong(offset + 1) // SecondMoveId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ContestCombos entity, int offset) {
        entity.setFirstMoveId(cursor.getLong(offset + 0));
        entity.setSecondMoveId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ContestCombos entity, long rowId) {
        entity.setFirstMoveId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ContestCombos entity) {
        if(entity != null) {
            return entity.getFirstMoveId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMovesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getMovesDao().getAllColumns());
            builder.append(" FROM contest_combos T");
            builder.append(" LEFT JOIN moves T0 ON T.\"first_move_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN moves T1 ON T.\"second_move_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ContestCombos loadCurrentDeep(Cursor cursor, boolean lock) {
        ContestCombos entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Moves FirstMove = loadCurrentOther(daoSession.getMovesDao(), cursor, offset);
         if(FirstMove != null) {
            entity.setFirstMove(FirstMove);
        }
        offset += daoSession.getMovesDao().getAllColumns().length;

        Moves SecondMove = loadCurrentOther(daoSession.getMovesDao(), cursor, offset);
         if(SecondMove != null) {
            entity.setSecondMove(SecondMove);
        }

        return entity;    
    }

    public ContestCombos loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ContestCombos> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ContestCombos> list = new ArrayList<ContestCombos>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ContestCombos> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ContestCombos> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
