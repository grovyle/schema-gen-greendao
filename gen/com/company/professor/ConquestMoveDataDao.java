package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ConquestMoveData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "conquest_move_data".
*/
public class ConquestMoveDataDao extends AbstractDao<ConquestMoveData, Long> {

    public static final String TABLENAME = "conquest_move_data";

    /**
     * Properties of entity ConquestMoveData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property MoveId = new Property(0, long.class, "MoveId", true, "move_id");
        public final static Property Power = new Property(1, Long.class, "Power", false, "power");
        public final static Property Accuracy = new Property(2, Long.class, "Accuracy", false, "accuracy");
        public final static Property EffectChance = new Property(3, Long.class, "EffectChance", false, "effect_chance");
        public final static Property EffectId = new Property(4, long.class, "EffectId", false, "effect_id");
        public final static Property RangeId = new Property(5, long.class, "RangeId", false, "range_id");
        public final static Property DisplacementId = new Property(6, Long.class, "DisplacementId", false, "displacement_id");
    };

    private DaoSession daoSession;


    public ConquestMoveDataDao(DaoConfig config) {
        super(config);
    }
    
    public ConquestMoveDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"conquest_move_data\" (" + //
                "\"move_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: MoveId
                "\"power\" INTEGER," + // 1: Power
                "\"accuracy\" INTEGER," + // 2: Accuracy
                "\"effect_chance\" INTEGER," + // 3: EffectChance
                "\"effect_id\" INTEGER NOT NULL ," + // 4: EffectId
                "\"range_id\" INTEGER NOT NULL ," + // 5: RangeId
                "\"displacement_id\" INTEGER);"); // 6: DisplacementId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"conquest_move_data\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConquestMoveData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getMoveId());
 
        Long Power = entity.getPower();
        if (Power != null) {
            stmt.bindLong(2, Power);
        }
 
        Long Accuracy = entity.getAccuracy();
        if (Accuracy != null) {
            stmt.bindLong(3, Accuracy);
        }
 
        Long EffectChance = entity.getEffectChance();
        if (EffectChance != null) {
            stmt.bindLong(4, EffectChance);
        }
        stmt.bindLong(5, entity.getEffectId());
        stmt.bindLong(6, entity.getRangeId());
 
        Long DisplacementId = entity.getDisplacementId();
        if (DisplacementId != null) {
            stmt.bindLong(7, DisplacementId);
        }
    }

    @Override
    protected void attachEntity(ConquestMoveData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ConquestMoveData readEntity(Cursor cursor, int offset) {
        ConquestMoveData entity = new ConquestMoveData( //
            cursor.getLong(offset + 0), // MoveId
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // Power
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // Accuracy
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // EffectChance
            cursor.getLong(offset + 4), // EffectId
            cursor.getLong(offset + 5), // RangeId
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6) // DisplacementId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConquestMoveData entity, int offset) {
        entity.setMoveId(cursor.getLong(offset + 0));
        entity.setPower(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setAccuracy(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setEffectChance(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setEffectId(cursor.getLong(offset + 4));
        entity.setRangeId(cursor.getLong(offset + 5));
        entity.setDisplacementId(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConquestMoveData entity, long rowId) {
        entity.setMoveId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ConquestMoveData entity) {
        if(entity != null) {
            return entity.getMoveId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMovesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getConquestMoveEffectsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getConquestMoveRangesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getConquestMoveDisplacementsDao().getAllColumns());
            builder.append(" FROM conquest_move_data T");
            builder.append(" LEFT JOIN moves T0 ON T.\"move_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN conquest_move_effects T1 ON T.\"effect_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN conquest_move_ranges T2 ON T.\"range_id\"=T2.\"id\"");
            builder.append(" LEFT JOIN conquest_move_displacements T3 ON T.\"displacement_id\"=T3.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ConquestMoveData loadCurrentDeep(Cursor cursor, boolean lock) {
        ConquestMoveData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Moves Moves = loadCurrentOther(daoSession.getMovesDao(), cursor, offset);
         if(Moves != null) {
            entity.setMoves(Moves);
        }
        offset += daoSession.getMovesDao().getAllColumns().length;

        ConquestMoveEffects ConquestMoveEffects = loadCurrentOther(daoSession.getConquestMoveEffectsDao(), cursor, offset);
         if(ConquestMoveEffects != null) {
            entity.setConquestMoveEffects(ConquestMoveEffects);
        }
        offset += daoSession.getConquestMoveEffectsDao().getAllColumns().length;

        ConquestMoveRanges ConquestMoveRanges = loadCurrentOther(daoSession.getConquestMoveRangesDao(), cursor, offset);
         if(ConquestMoveRanges != null) {
            entity.setConquestMoveRanges(ConquestMoveRanges);
        }
        offset += daoSession.getConquestMoveRangesDao().getAllColumns().length;

        ConquestMoveDisplacements ConquestMoveDisplacements = loadCurrentOther(daoSession.getConquestMoveDisplacementsDao(), cursor, offset);
        entity.setConquestMoveDisplacements(ConquestMoveDisplacements);

        return entity;    
    }

    public ConquestMoveData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ConquestMoveData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ConquestMoveData> list = new ArrayList<ConquestMoveData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ConquestMoveData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ConquestMoveData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
