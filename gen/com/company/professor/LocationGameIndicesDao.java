package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.LocationGameIndices;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "location_game_indices".
*/
public class LocationGameIndicesDao extends AbstractDao<LocationGameIndices, Long> {

    public static final String TABLENAME = "location_game_indices";

    /**
     * Properties of entity LocationGameIndices.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property LocationId = new Property(0, long.class, "LocationId", true, "location_id");
        public final static Property GenerationId = new Property(1, long.class, "GenerationId", false, "generation_id");
        public final static Property GameIndex = new Property(2, long.class, "GameIndex", false, "game_index");
    };

    private DaoSession daoSession;


    public LocationGameIndicesDao(DaoConfig config) {
        super(config);
    }
    
    public LocationGameIndicesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"location_game_indices\" (" + //
                "\"location_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: LocationId
                "\"generation_id\" INTEGER NOT NULL ," + // 1: GenerationId
                "\"game_index\" INTEGER NOT NULL );"); // 2: GameIndex
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_location_game_indices_location_id_generation_id_game_index ON location_game_indices" +
                " (\"location_id\",\"generation_id\",\"game_index\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"location_game_indices\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, LocationGameIndices entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getLocationId());
        stmt.bindLong(2, entity.getGenerationId());
        stmt.bindLong(3, entity.getGameIndex());
    }

    @Override
    protected void attachEntity(LocationGameIndices entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public LocationGameIndices readEntity(Cursor cursor, int offset) {
        LocationGameIndices entity = new LocationGameIndices( //
            cursor.getLong(offset + 0), // LocationId
            cursor.getLong(offset + 1), // GenerationId
            cursor.getLong(offset + 2) // GameIndex
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, LocationGameIndices entity, int offset) {
        entity.setLocationId(cursor.getLong(offset + 0));
        entity.setGenerationId(cursor.getLong(offset + 1));
        entity.setGameIndex(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(LocationGameIndices entity, long rowId) {
        entity.setLocationId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(LocationGameIndices entity) {
        if(entity != null) {
            return entity.getLocationId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getLocationsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getGenerationsDao().getAllColumns());
            builder.append(" FROM location_game_indices T");
            builder.append(" LEFT JOIN locations T0 ON T.\"location_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN generations T1 ON T.\"generation_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected LocationGameIndices loadCurrentDeep(Cursor cursor, boolean lock) {
        LocationGameIndices entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Locations Locations = loadCurrentOther(daoSession.getLocationsDao(), cursor, offset);
         if(Locations != null) {
            entity.setLocations(Locations);
        }
        offset += daoSession.getLocationsDao().getAllColumns().length;

        Generations Generations = loadCurrentOther(daoSession.getGenerationsDao(), cursor, offset);
         if(Generations != null) {
            entity.setGenerations(Generations);
        }

        return entity;    
    }

    public LocationGameIndices loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<LocationGameIndices> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<LocationGameIndices> list = new ArrayList<LocationGameIndices>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<LocationGameIndices> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<LocationGameIndices> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
