package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.VersionGroupPokemonMoveMethods;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "version_group_pokemon_move_methods".
*/
public class VersionGroupPokemonMoveMethodsDao extends AbstractDao<VersionGroupPokemonMoveMethods, Long> {

    public static final String TABLENAME = "version_group_pokemon_move_methods";

    /**
     * Properties of entity VersionGroupPokemonMoveMethods.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property VersionGroupId = new Property(0, long.class, "VersionGroupId", true, "version_group_id");
        public final static Property PokemonMoveMethodId = new Property(1, long.class, "PokemonMoveMethodId", false, "pokemon_move_method_id");
    };

    private DaoSession daoSession;


    public VersionGroupPokemonMoveMethodsDao(DaoConfig config) {
        super(config);
    }
    
    public VersionGroupPokemonMoveMethodsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"version_group_pokemon_move_methods\" (" + //
                "\"version_group_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: VersionGroupId
                "\"pokemon_move_method_id\" INTEGER NOT NULL );"); // 1: PokemonMoveMethodId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_version_group_pokemon_move_methods_version_group_id_pokemon_move_method_id ON version_group_pokemon_move_methods" +
                " (\"version_group_id\",\"pokemon_move_method_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"version_group_pokemon_move_methods\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, VersionGroupPokemonMoveMethods entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getVersionGroupId());
        stmt.bindLong(2, entity.getPokemonMoveMethodId());
    }

    @Override
    protected void attachEntity(VersionGroupPokemonMoveMethods entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public VersionGroupPokemonMoveMethods readEntity(Cursor cursor, int offset) {
        VersionGroupPokemonMoveMethods entity = new VersionGroupPokemonMoveMethods( //
            cursor.getLong(offset + 0), // VersionGroupId
            cursor.getLong(offset + 1) // PokemonMoveMethodId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, VersionGroupPokemonMoveMethods entity, int offset) {
        entity.setVersionGroupId(cursor.getLong(offset + 0));
        entity.setPokemonMoveMethodId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(VersionGroupPokemonMoveMethods entity, long rowId) {
        entity.setVersionGroupId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(VersionGroupPokemonMoveMethods entity) {
        if(entity != null) {
            return entity.getVersionGroupId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getVersionGroupsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPokemonMoveMethodsDao().getAllColumns());
            builder.append(" FROM version_group_pokemon_move_methods T");
            builder.append(" LEFT JOIN version_groups T0 ON T.\"version_group_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN pokemon_move_methods T1 ON T.\"pokemon_move_method_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected VersionGroupPokemonMoveMethods loadCurrentDeep(Cursor cursor, boolean lock) {
        VersionGroupPokemonMoveMethods entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        VersionGroups VersionGroups = loadCurrentOther(daoSession.getVersionGroupsDao(), cursor, offset);
         if(VersionGroups != null) {
            entity.setVersionGroups(VersionGroups);
        }
        offset += daoSession.getVersionGroupsDao().getAllColumns().length;

        PokemonMoveMethods PokemonMoveMethods = loadCurrentOther(daoSession.getPokemonMoveMethodsDao(), cursor, offset);
         if(PokemonMoveMethods != null) {
            entity.setPokemonMoveMethods(PokemonMoveMethods);
        }

        return entity;    
    }

    public VersionGroupPokemonMoveMethods loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<VersionGroupPokemonMoveMethods> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<VersionGroupPokemonMoveMethods> list = new ArrayList<VersionGroupPokemonMoveMethods>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<VersionGroupPokemonMoveMethods> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<VersionGroupPokemonMoveMethods> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
