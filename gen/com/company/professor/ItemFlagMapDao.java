package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ItemFlagMap;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "item_flag_map".
*/
public class ItemFlagMapDao extends AbstractDao<ItemFlagMap, Long> {

    public static final String TABLENAME = "item_flag_map";

    /**
     * Properties of entity ItemFlagMap.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property ItemId = new Property(0, long.class, "ItemId", true, "item_id");
        public final static Property ItemFlagId = new Property(1, long.class, "ItemFlagId", false, "item_flag_id");
    };

    private DaoSession daoSession;


    public ItemFlagMapDao(DaoConfig config) {
        super(config);
    }
    
    public ItemFlagMapDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"item_flag_map\" (" + //
                "\"item_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: ItemId
                "\"item_flag_id\" INTEGER NOT NULL );"); // 1: ItemFlagId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_item_flag_map_item_id_item_flag_id ON item_flag_map" +
                " (\"item_id\",\"item_flag_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"item_flag_map\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ItemFlagMap entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getItemId());
        stmt.bindLong(2, entity.getItemFlagId());
    }

    @Override
    protected void attachEntity(ItemFlagMap entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ItemFlagMap readEntity(Cursor cursor, int offset) {
        ItemFlagMap entity = new ItemFlagMap( //
            cursor.getLong(offset + 0), // ItemId
            cursor.getLong(offset + 1) // ItemFlagId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ItemFlagMap entity, int offset) {
        entity.setItemId(cursor.getLong(offset + 0));
        entity.setItemFlagId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ItemFlagMap entity, long rowId) {
        entity.setItemId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ItemFlagMap entity) {
        if(entity != null) {
            return entity.getItemId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getItemsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getItemFlagsDao().getAllColumns());
            builder.append(" FROM item_flag_map T");
            builder.append(" LEFT JOIN items T0 ON T.\"item_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN item_flags T1 ON T.\"item_flag_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ItemFlagMap loadCurrentDeep(Cursor cursor, boolean lock) {
        ItemFlagMap entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Items Items = loadCurrentOther(daoSession.getItemsDao(), cursor, offset);
         if(Items != null) {
            entity.setItems(Items);
        }
        offset += daoSession.getItemsDao().getAllColumns().length;

        ItemFlags ItemFlags = loadCurrentOther(daoSession.getItemFlagsDao(), cursor, offset);
         if(ItemFlags != null) {
            entity.setItemFlags(ItemFlags);
        }

        return entity;    
    }

    public ItemFlagMap loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ItemFlagMap> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ItemFlagMap> list = new ArrayList<ItemFlagMap>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ItemFlagMap> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ItemFlagMap> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
