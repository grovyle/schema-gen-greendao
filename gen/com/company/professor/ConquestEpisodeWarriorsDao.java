package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ConquestEpisodeWarriors;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "conquest_episode_warriors".
*/
public class ConquestEpisodeWarriorsDao extends AbstractDao<ConquestEpisodeWarriors, Long> {

    public static final String TABLENAME = "conquest_episode_warriors";

    /**
     * Properties of entity ConquestEpisodeWarriors.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property EpisodeId = new Property(0, long.class, "EpisodeId", true, "episode_id");
        public final static Property WarriorId = new Property(1, long.class, "WarriorId", false, "warrior_id");
    };

    private DaoSession daoSession;


    public ConquestEpisodeWarriorsDao(DaoConfig config) {
        super(config);
    }
    
    public ConquestEpisodeWarriorsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"conquest_episode_warriors\" (" + //
                "\"episode_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: EpisodeId
                "\"warrior_id\" INTEGER NOT NULL );"); // 1: WarriorId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_conquest_episode_warriors_episode_id_warrior_id ON conquest_episode_warriors" +
                " (\"episode_id\",\"warrior_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"conquest_episode_warriors\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConquestEpisodeWarriors entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getEpisodeId());
        stmt.bindLong(2, entity.getWarriorId());
    }

    @Override
    protected void attachEntity(ConquestEpisodeWarriors entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ConquestEpisodeWarriors readEntity(Cursor cursor, int offset) {
        ConquestEpisodeWarriors entity = new ConquestEpisodeWarriors( //
            cursor.getLong(offset + 0), // EpisodeId
            cursor.getLong(offset + 1) // WarriorId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConquestEpisodeWarriors entity, int offset) {
        entity.setEpisodeId(cursor.getLong(offset + 0));
        entity.setWarriorId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConquestEpisodeWarriors entity, long rowId) {
        entity.setEpisodeId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ConquestEpisodeWarriors entity) {
        if(entity != null) {
            return entity.getEpisodeId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getConquestEpisodesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getConquestWarriorsDao().getAllColumns());
            builder.append(" FROM conquest_episode_warriors T");
            builder.append(" LEFT JOIN conquest_episodes T0 ON T.\"episode_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN conquest_warriors T1 ON T.\"warrior_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ConquestEpisodeWarriors loadCurrentDeep(Cursor cursor, boolean lock) {
        ConquestEpisodeWarriors entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ConquestEpisodes ConquestEpisodes = loadCurrentOther(daoSession.getConquestEpisodesDao(), cursor, offset);
         if(ConquestEpisodes != null) {
            entity.setConquestEpisodes(ConquestEpisodes);
        }
        offset += daoSession.getConquestEpisodesDao().getAllColumns().length;

        ConquestWarriors ConquestWarriors = loadCurrentOther(daoSession.getConquestWarriorsDao(), cursor, offset);
         if(ConquestWarriors != null) {
            entity.setConquestWarriors(ConquestWarriors);
        }

        return entity;    
    }

    public ConquestEpisodeWarriors loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ConquestEpisodeWarriors> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ConquestEpisodeWarriors> list = new ArrayList<ConquestEpisodeWarriors>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ConquestEpisodeWarriors> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ConquestEpisodeWarriors> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
