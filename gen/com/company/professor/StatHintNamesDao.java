package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.StatHintNames;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "stat_hint_names".
*/
public class StatHintNamesDao extends AbstractDao<StatHintNames, Long> {

    public static final String TABLENAME = "stat_hint_names";

    /**
     * Properties of entity StatHintNames.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property StatHintId = new Property(0, long.class, "StatHintId", true, "stat_hint_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property Message = new Property(2, String.class, "Message", false, "message");
    };

    private DaoSession daoSession;


    public StatHintNamesDao(DaoConfig config) {
        super(config);
    }
    
    public StatHintNamesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"stat_hint_names\" (" + //
                "\"stat_hint_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: StatHintId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"message\" TEXT NOT NULL );"); // 2: Message
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_stat_hint_names_stat_hint_id_local_language_id ON stat_hint_names" +
                " (\"stat_hint_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"stat_hint_names\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, StatHintNames entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getStatHintId());
        stmt.bindLong(2, entity.getLocalLanguageId());
        stmt.bindString(3, entity.getMessage());
    }

    @Override
    protected void attachEntity(StatHintNames entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public StatHintNames readEntity(Cursor cursor, int offset) {
        StatHintNames entity = new StatHintNames( //
            cursor.getLong(offset + 0), // StatHintId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.getString(offset + 2) // Message
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, StatHintNames entity, int offset) {
        entity.setStatHintId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setMessage(cursor.getString(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(StatHintNames entity, long rowId) {
        entity.setStatHintId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(StatHintNames entity) {
        if(entity != null) {
            return entity.getStatHintId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getStatHintsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM stat_hint_names T");
            builder.append(" LEFT JOIN stat_hints T0 ON T.\"stat_hint_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected StatHintNames loadCurrentDeep(Cursor cursor, boolean lock) {
        StatHintNames entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        StatHints StatHints = loadCurrentOther(daoSession.getStatHintsDao(), cursor, offset);
         if(StatHints != null) {
            entity.setStatHints(StatHints);
        }
        offset += daoSession.getStatHintsDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public StatHintNames loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<StatHintNames> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<StatHintNames> list = new ArrayList<StatHintNames>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<StatHintNames> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<StatHintNames> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
