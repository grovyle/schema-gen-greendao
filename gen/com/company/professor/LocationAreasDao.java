package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.LocationAreas;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "location_areas".
*/
public class LocationAreasDao extends AbstractDao<LocationAreas, Long> {

    public static final String TABLENAME = "location_areas";

    /**
     * Properties of entity LocationAreas.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property LocationId = new Property(1, long.class, "LocationId", false, "location_id");
        public final static Property GameIndex = new Property(2, long.class, "GameIndex", false, "game_index");
        public final static Property Identifier = new Property(3, String.class, "Identifier", false, "identifier");
    };

    private DaoSession daoSession;


    public LocationAreasDao(DaoConfig config) {
        super(config);
    }
    
    public LocationAreasDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"location_areas\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"location_id\" INTEGER NOT NULL ," + // 1: LocationId
                "\"game_index\" INTEGER NOT NULL ," + // 2: GameIndex
                "\"identifier\" TEXT);"); // 3: Identifier
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"location_areas\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, LocationAreas entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getLocationId());
        stmt.bindLong(3, entity.getGameIndex());
 
        String Identifier = entity.getIdentifier();
        if (Identifier != null) {
            stmt.bindString(4, Identifier);
        }
    }

    @Override
    protected void attachEntity(LocationAreas entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public LocationAreas readEntity(Cursor cursor, int offset) {
        LocationAreas entity = new LocationAreas( //
            cursor.getLong(offset + 0), // Id
            cursor.getLong(offset + 1), // LocationId
            cursor.getLong(offset + 2), // GameIndex
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // Identifier
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, LocationAreas entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setLocationId(cursor.getLong(offset + 1));
        entity.setGameIndex(cursor.getLong(offset + 2));
        entity.setIdentifier(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(LocationAreas entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(LocationAreas entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getLocationsDao().getAllColumns());
            builder.append(" FROM location_areas T");
            builder.append(" LEFT JOIN locations T0 ON T.\"location_id\"=T0.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected LocationAreas loadCurrentDeep(Cursor cursor, boolean lock) {
        LocationAreas entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Locations Locations = loadCurrentOther(daoSession.getLocationsDao(), cursor, offset);
         if(Locations != null) {
            entity.setLocations(Locations);
        }

        return entity;    
    }

    public LocationAreas loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<LocationAreas> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<LocationAreas> list = new ArrayList<LocationAreas>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<LocationAreas> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<LocationAreas> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
