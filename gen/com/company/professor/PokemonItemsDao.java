package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonItems;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_items".
*/
public class PokemonItemsDao extends AbstractDao<PokemonItems, Long> {

    public static final String TABLENAME = "pokemon_items";

    /**
     * Properties of entity PokemonItems.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property PokemonId = new Property(0, long.class, "PokemonId", true, "pokemon_id");
        public final static Property VersionId = new Property(1, long.class, "VersionId", false, "version_id");
        public final static Property ItemId = new Property(2, long.class, "ItemId", false, "item_id");
        public final static Property Rarity = new Property(3, long.class, "Rarity", false, "rarity");
    };

    private DaoSession daoSession;


    public PokemonItemsDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonItemsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_items\" (" + //
                "\"pokemon_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: PokemonId
                "\"version_id\" INTEGER NOT NULL ," + // 1: VersionId
                "\"item_id\" INTEGER NOT NULL ," + // 2: ItemId
                "\"rarity\" INTEGER NOT NULL );"); // 3: Rarity
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_items_pokemon_id_version_id_item_id ON pokemon_items" +
                " (\"pokemon_id\",\"version_id\",\"item_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_items\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonItems entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getPokemonId());
        stmt.bindLong(2, entity.getVersionId());
        stmt.bindLong(3, entity.getItemId());
        stmt.bindLong(4, entity.getRarity());
    }

    @Override
    protected void attachEntity(PokemonItems entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonItems readEntity(Cursor cursor, int offset) {
        PokemonItems entity = new PokemonItems( //
            cursor.getLong(offset + 0), // PokemonId
            cursor.getLong(offset + 1), // VersionId
            cursor.getLong(offset + 2), // ItemId
            cursor.getLong(offset + 3) // Rarity
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonItems entity, int offset) {
        entity.setPokemonId(cursor.getLong(offset + 0));
        entity.setVersionId(cursor.getLong(offset + 1));
        entity.setItemId(cursor.getLong(offset + 2));
        entity.setRarity(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonItems entity, long rowId) {
        entity.setPokemonId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonItems entity) {
        if(entity != null) {
            return entity.getPokemonId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVersionsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getItemsDao().getAllColumns());
            builder.append(" FROM pokemon_items T");
            builder.append(" LEFT JOIN pokemon T0 ON T.\"pokemon_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN versions T1 ON T.\"version_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN items T2 ON T.\"item_id\"=T2.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonItems loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonItems entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Pokemon Pokemon = loadCurrentOther(daoSession.getPokemonDao(), cursor, offset);
         if(Pokemon != null) {
            entity.setPokemon(Pokemon);
        }
        offset += daoSession.getPokemonDao().getAllColumns().length;

        Versions Versions = loadCurrentOther(daoSession.getVersionsDao(), cursor, offset);
         if(Versions != null) {
            entity.setVersions(Versions);
        }
        offset += daoSession.getVersionsDao().getAllColumns().length;

        Items Items = loadCurrentOther(daoSession.getItemsDao(), cursor, offset);
         if(Items != null) {
            entity.setItems(Items);
        }

        return entity;    
    }

    public PokemonItems loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonItems> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonItems> list = new ArrayList<PokemonItems>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonItems> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonItems> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
