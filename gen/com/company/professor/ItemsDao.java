package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.Items;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "items".
*/
public class ItemsDao extends AbstractDao<Items, Long> {

    public static final String TABLENAME = "items";

    /**
     * Properties of entity Items.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property Identifier = new Property(1, String.class, "Identifier", false, "identifier");
        public final static Property CategoryId = new Property(2, long.class, "CategoryId", false, "category_id");
        public final static Property Cost = new Property(3, long.class, "Cost", false, "cost");
        public final static Property FlingPower = new Property(4, Long.class, "FlingPower", false, "fling_power");
        public final static Property FlingEffectId = new Property(5, Long.class, "FlingEffectId", false, "fling_effect_id");
    };

    private DaoSession daoSession;


    public ItemsDao(DaoConfig config) {
        super(config);
    }
    
    public ItemsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"items\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"identifier\" TEXT NOT NULL ," + // 1: Identifier
                "\"category_id\" INTEGER NOT NULL ," + // 2: CategoryId
                "\"cost\" INTEGER NOT NULL ," + // 3: Cost
                "\"fling_power\" INTEGER," + // 4: FlingPower
                "\"fling_effect_id\" INTEGER);"); // 5: FlingEffectId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"items\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Items entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindString(2, entity.getIdentifier());
        stmt.bindLong(3, entity.getCategoryId());
        stmt.bindLong(4, entity.getCost());
 
        Long FlingPower = entity.getFlingPower();
        if (FlingPower != null) {
            stmt.bindLong(5, FlingPower);
        }
 
        Long FlingEffectId = entity.getFlingEffectId();
        if (FlingEffectId != null) {
            stmt.bindLong(6, FlingEffectId);
        }
    }

    @Override
    protected void attachEntity(Items entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Items readEntity(Cursor cursor, int offset) {
        Items entity = new Items( //
            cursor.getLong(offset + 0), // Id
            cursor.getString(offset + 1), // Identifier
            cursor.getLong(offset + 2), // CategoryId
            cursor.getLong(offset + 3), // Cost
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // FlingPower
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5) // FlingEffectId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Items entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setIdentifier(cursor.getString(offset + 1));
        entity.setCategoryId(cursor.getLong(offset + 2));
        entity.setCost(cursor.getLong(offset + 3));
        entity.setFlingPower(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setFlingEffectId(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Items entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Items entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getItemCategoriesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getItemFlingEffectsDao().getAllColumns());
            builder.append(" FROM items T");
            builder.append(" LEFT JOIN item_categories T0 ON T.\"category_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN item_fling_effects T1 ON T.\"fling_effect_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Items loadCurrentDeep(Cursor cursor, boolean lock) {
        Items entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ItemCategories ItemCategories = loadCurrentOther(daoSession.getItemCategoriesDao(), cursor, offset);
         if(ItemCategories != null) {
            entity.setItemCategories(ItemCategories);
        }
        offset += daoSession.getItemCategoriesDao().getAllColumns().length;

        ItemFlingEffects ItemFlingEffects = loadCurrentOther(daoSession.getItemFlingEffectsDao(), cursor, offset);
        entity.setItemFlingEffects(ItemFlingEffects);

        return entity;    
    }

    public Items loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Items> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Items> list = new ArrayList<Items>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Items> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Items> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
