package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.Stats;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "stats".
*/
public class StatsDao extends AbstractDao<Stats, Long> {

    public static final String TABLENAME = "stats";

    /**
     * Properties of entity Stats.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property DamageClassId = new Property(1, Long.class, "DamageClassId", false, "damage_class_id");
        public final static Property Identifier = new Property(2, String.class, "Identifier", false, "identifier");
        public final static Property IsBattleOnly = new Property(3, boolean.class, "IsBattleOnly", false, "is_battle_only");
        public final static Property GameIndex = new Property(4, Long.class, "GameIndex", false, "game_index");
    };

    private DaoSession daoSession;


    public StatsDao(DaoConfig config) {
        super(config);
    }
    
    public StatsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"stats\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"damage_class_id\" INTEGER," + // 1: DamageClassId
                "\"identifier\" TEXT NOT NULL ," + // 2: Identifier
                "\"is_battle_only\" INTEGER NOT NULL ," + // 3: IsBattleOnly
                "\"game_index\" INTEGER);"); // 4: GameIndex
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"stats\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Stats entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        Long DamageClassId = entity.getDamageClassId();
        if (DamageClassId != null) {
            stmt.bindLong(2, DamageClassId);
        }
        stmt.bindString(3, entity.getIdentifier());
        stmt.bindLong(4, entity.getIsBattleOnly() ? 1L: 0L);
 
        Long GameIndex = entity.getGameIndex();
        if (GameIndex != null) {
            stmt.bindLong(5, GameIndex);
        }
    }

    @Override
    protected void attachEntity(Stats entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Stats readEntity(Cursor cursor, int offset) {
        Stats entity = new Stats( //
            cursor.getLong(offset + 0), // Id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // DamageClassId
            cursor.getString(offset + 2), // Identifier
            cursor.getShort(offset + 3) != 0, // IsBattleOnly
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4) // GameIndex
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Stats entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setDamageClassId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setIdentifier(cursor.getString(offset + 2));
        entity.setIsBattleOnly(cursor.getShort(offset + 3) != 0);
        entity.setGameIndex(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Stats entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Stats entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMoveDamageClassesDao().getAllColumns());
            builder.append(" FROM stats T");
            builder.append(" LEFT JOIN move_damage_classes T0 ON T.\"damage_class_id\"=T0.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Stats loadCurrentDeep(Cursor cursor, boolean lock) {
        Stats entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        MoveDamageClasses MoveDamageClasses = loadCurrentOther(daoSession.getMoveDamageClassesDao(), cursor, offset);
        entity.setMoveDamageClasses(MoveDamageClasses);

        return entity;    
    }

    public Stats loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Stats> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Stats> list = new ArrayList<Stats>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Stats> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Stats> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
