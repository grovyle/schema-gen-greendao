package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.Pokemon;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon".
*/
public class PokemonDao extends AbstractDao<Pokemon, Long> {

    public static final String TABLENAME = "pokemon";

    /**
     * Properties of entity Pokemon.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property Identifier = new Property(1, String.class, "Identifier", false, "identifier");
        public final static Property SpeciesId = new Property(2, Long.class, "SpeciesId", false, "species_id");
        public final static Property Height = new Property(3, long.class, "Height", false, "height");
        public final static Property Weight = new Property(4, long.class, "Weight", false, "weight");
        public final static Property BaseExperience = new Property(5, long.class, "BaseExperience", false, "base_experience");
        public final static Property Order = new Property(6, long.class, "Order", false, "order");
        public final static Property IsDefault = new Property(7, boolean.class, "IsDefault", false, "is_default");
    };

    private DaoSession daoSession;


    public PokemonDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"identifier\" TEXT NOT NULL ," + // 1: Identifier
                "\"species_id\" INTEGER," + // 2: SpeciesId
                "\"height\" INTEGER NOT NULL ," + // 3: Height
                "\"weight\" INTEGER NOT NULL ," + // 4: Weight
                "\"base_experience\" INTEGER NOT NULL ," + // 5: BaseExperience
                "\"order\" INTEGER NOT NULL ," + // 6: Order
                "\"is_default\" INTEGER NOT NULL );"); // 7: IsDefault
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Pokemon entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindString(2, entity.getIdentifier());
 
        Long SpeciesId = entity.getSpeciesId();
        if (SpeciesId != null) {
            stmt.bindLong(3, SpeciesId);
        }
        stmt.bindLong(4, entity.getHeight());
        stmt.bindLong(5, entity.getWeight());
        stmt.bindLong(6, entity.getBaseExperience());
        stmt.bindLong(7, entity.getOrder());
        stmt.bindLong(8, entity.getIsDefault() ? 1L: 0L);
    }

    @Override
    protected void attachEntity(Pokemon entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Pokemon readEntity(Cursor cursor, int offset) {
        Pokemon entity = new Pokemon( //
            cursor.getLong(offset + 0), // Id
            cursor.getString(offset + 1), // Identifier
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // SpeciesId
            cursor.getLong(offset + 3), // Height
            cursor.getLong(offset + 4), // Weight
            cursor.getLong(offset + 5), // BaseExperience
            cursor.getLong(offset + 6), // Order
            cursor.getShort(offset + 7) != 0 // IsDefault
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Pokemon entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setIdentifier(cursor.getString(offset + 1));
        entity.setSpeciesId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setHeight(cursor.getLong(offset + 3));
        entity.setWeight(cursor.getLong(offset + 4));
        entity.setBaseExperience(cursor.getLong(offset + 5));
        entity.setOrder(cursor.getLong(offset + 6));
        entity.setIsDefault(cursor.getShort(offset + 7) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Pokemon entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Pokemon entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonSpeciesDao().getAllColumns());
            builder.append(" FROM pokemon T");
            builder.append(" LEFT JOIN pokemon_species T0 ON T.\"species_id\"=T0.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Pokemon loadCurrentDeep(Cursor cursor, boolean lock) {
        Pokemon entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PokemonSpecies PokemonSpecies = loadCurrentOther(daoSession.getPokemonSpeciesDao(), cursor, offset);
        entity.setPokemonSpecies(PokemonSpecies);

        return entity;    
    }

    public Pokemon loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Pokemon> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Pokemon> list = new ArrayList<Pokemon>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Pokemon> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Pokemon> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
