package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.Encounters;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "encounters".
*/
public class EncountersDao extends AbstractDao<Encounters, Long> {

    public static final String TABLENAME = "encounters";

    /**
     * Properties of entity Encounters.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property VersionId = new Property(1, long.class, "VersionId", false, "version_id");
        public final static Property LocationAreaId = new Property(2, long.class, "LocationAreaId", false, "location_area_id");
        public final static Property EncounterSlotId = new Property(3, long.class, "EncounterSlotId", false, "encounter_slot_id");
        public final static Property PokemonId = new Property(4, long.class, "PokemonId", false, "pokemon_id");
        public final static Property MinLevel = new Property(5, long.class, "MinLevel", false, "min_level");
        public final static Property MaxLevel = new Property(6, long.class, "MaxLevel", false, "max_level");
    };

    private DaoSession daoSession;


    public EncountersDao(DaoConfig config) {
        super(config);
    }
    
    public EncountersDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"encounters\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"version_id\" INTEGER NOT NULL ," + // 1: VersionId
                "\"location_area_id\" INTEGER NOT NULL ," + // 2: LocationAreaId
                "\"encounter_slot_id\" INTEGER NOT NULL ," + // 3: EncounterSlotId
                "\"pokemon_id\" INTEGER NOT NULL ," + // 4: PokemonId
                "\"min_level\" INTEGER NOT NULL ," + // 5: MinLevel
                "\"max_level\" INTEGER NOT NULL );"); // 6: MaxLevel
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"encounters\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Encounters entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getVersionId());
        stmt.bindLong(3, entity.getLocationAreaId());
        stmt.bindLong(4, entity.getEncounterSlotId());
        stmt.bindLong(5, entity.getPokemonId());
        stmt.bindLong(6, entity.getMinLevel());
        stmt.bindLong(7, entity.getMaxLevel());
    }

    @Override
    protected void attachEntity(Encounters entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Encounters readEntity(Cursor cursor, int offset) {
        Encounters entity = new Encounters( //
            cursor.getLong(offset + 0), // Id
            cursor.getLong(offset + 1), // VersionId
            cursor.getLong(offset + 2), // LocationAreaId
            cursor.getLong(offset + 3), // EncounterSlotId
            cursor.getLong(offset + 4), // PokemonId
            cursor.getLong(offset + 5), // MinLevel
            cursor.getLong(offset + 6) // MaxLevel
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Encounters entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setVersionId(cursor.getLong(offset + 1));
        entity.setLocationAreaId(cursor.getLong(offset + 2));
        entity.setEncounterSlotId(cursor.getLong(offset + 3));
        entity.setPokemonId(cursor.getLong(offset + 4));
        entity.setMinLevel(cursor.getLong(offset + 5));
        entity.setMaxLevel(cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Encounters entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Encounters entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getVersionsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLocationAreasDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getEncounterSlotsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getPokemonDao().getAllColumns());
            builder.append(" FROM encounters T");
            builder.append(" LEFT JOIN versions T0 ON T.\"version_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN location_areas T1 ON T.\"location_area_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN encounter_slots T2 ON T.\"encounter_slot_id\"=T2.\"id\"");
            builder.append(" LEFT JOIN pokemon T3 ON T.\"pokemon_id\"=T3.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Encounters loadCurrentDeep(Cursor cursor, boolean lock) {
        Encounters entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Versions Versions = loadCurrentOther(daoSession.getVersionsDao(), cursor, offset);
         if(Versions != null) {
            entity.setVersions(Versions);
        }
        offset += daoSession.getVersionsDao().getAllColumns().length;

        LocationAreas LocationAreas = loadCurrentOther(daoSession.getLocationAreasDao(), cursor, offset);
         if(LocationAreas != null) {
            entity.setLocationAreas(LocationAreas);
        }
        offset += daoSession.getLocationAreasDao().getAllColumns().length;

        EncounterSlots EncounterSlots = loadCurrentOther(daoSession.getEncounterSlotsDao(), cursor, offset);
         if(EncounterSlots != null) {
            entity.setEncounterSlots(EncounterSlots);
        }
        offset += daoSession.getEncounterSlotsDao().getAllColumns().length;

        Pokemon Pokemon = loadCurrentOther(daoSession.getPokemonDao(), cursor, offset);
         if(Pokemon != null) {
            entity.setPokemon(Pokemon);
        }

        return entity;    
    }

    public Encounters loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Encounters> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Encounters> list = new ArrayList<Encounters>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Encounters> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Encounters> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
