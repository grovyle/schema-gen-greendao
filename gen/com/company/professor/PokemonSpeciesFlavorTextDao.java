package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonSpeciesFlavorText;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_species_flavor_text".
*/
public class PokemonSpeciesFlavorTextDao extends AbstractDao<PokemonSpeciesFlavorText, Long> {

    public static final String TABLENAME = "pokemon_species_flavor_text";

    /**
     * Properties of entity PokemonSpeciesFlavorText.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property SpeciesId = new Property(0, long.class, "SpeciesId", true, "species_id");
        public final static Property VersionId = new Property(1, long.class, "VersionId", false, "version_id");
        public final static Property LanguageId = new Property(2, long.class, "LanguageId", false, "language_id");
        public final static Property FlavorText = new Property(3, String.class, "FlavorText", false, "flavor_text");
    };

    private DaoSession daoSession;


    public PokemonSpeciesFlavorTextDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonSpeciesFlavorTextDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_species_flavor_text\" (" + //
                "\"species_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: SpeciesId
                "\"version_id\" INTEGER NOT NULL ," + // 1: VersionId
                "\"language_id\" INTEGER NOT NULL ," + // 2: LanguageId
                "\"flavor_text\" TEXT NOT NULL );"); // 3: FlavorText
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_species_flavor_text_species_id_version_id_language_id ON pokemon_species_flavor_text" +
                " (\"species_id\",\"version_id\",\"language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_species_flavor_text\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonSpeciesFlavorText entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getSpeciesId());
        stmt.bindLong(2, entity.getVersionId());
        stmt.bindLong(3, entity.getLanguageId());
        stmt.bindString(4, entity.getFlavorText());
    }

    @Override
    protected void attachEntity(PokemonSpeciesFlavorText entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonSpeciesFlavorText readEntity(Cursor cursor, int offset) {
        PokemonSpeciesFlavorText entity = new PokemonSpeciesFlavorText( //
            cursor.getLong(offset + 0), // SpeciesId
            cursor.getLong(offset + 1), // VersionId
            cursor.getLong(offset + 2), // LanguageId
            cursor.getString(offset + 3) // FlavorText
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonSpeciesFlavorText entity, int offset) {
        entity.setSpeciesId(cursor.getLong(offset + 0));
        entity.setVersionId(cursor.getLong(offset + 1));
        entity.setLanguageId(cursor.getLong(offset + 2));
        entity.setFlavorText(cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonSpeciesFlavorText entity, long rowId) {
        entity.setSpeciesId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonSpeciesFlavorText entity) {
        if(entity != null) {
            return entity.getSpeciesId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonSpeciesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVersionsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM pokemon_species_flavor_text T");
            builder.append(" LEFT JOIN pokemon_species T0 ON T.\"species_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN versions T1 ON T.\"version_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN languages T2 ON T.\"language_id\"=T2.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonSpeciesFlavorText loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonSpeciesFlavorText entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PokemonSpecies PokemonSpecies = loadCurrentOther(daoSession.getPokemonSpeciesDao(), cursor, offset);
         if(PokemonSpecies != null) {
            entity.setPokemonSpecies(PokemonSpecies);
        }
        offset += daoSession.getPokemonSpeciesDao().getAllColumns().length;

        Versions Versions = loadCurrentOther(daoSession.getVersionsDao(), cursor, offset);
         if(Versions != null) {
            entity.setVersions(Versions);
        }
        offset += daoSession.getVersionsDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public PokemonSpeciesFlavorText loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonSpeciesFlavorText> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonSpeciesFlavorText> list = new ArrayList<PokemonSpeciesFlavorText>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonSpeciesFlavorText> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonSpeciesFlavorText> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
