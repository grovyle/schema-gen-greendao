package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.MoveEffectChangelog;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "move_effect_changelog".
*/
public class MoveEffectChangelogDao extends AbstractDao<MoveEffectChangelog, Long> {

    public static final String TABLENAME = "move_effect_changelog";

    /**
     * Properties of entity MoveEffectChangelog.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property EffectId = new Property(1, long.class, "EffectId", false, "effect_id");
        public final static Property ChangedInVersionGroupId = new Property(2, long.class, "ChangedInVersionGroupId", false, "changed_in_version_group_id");
    };

    private DaoSession daoSession;


    public MoveEffectChangelogDao(DaoConfig config) {
        super(config);
    }
    
    public MoveEffectChangelogDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"move_effect_changelog\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"effect_id\" INTEGER NOT NULL ," + // 1: EffectId
                "\"changed_in_version_group_id\" INTEGER NOT NULL );"); // 2: ChangedInVersionGroupId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_move_effect_changelog_effect_id_changed_in_version_group_id ON move_effect_changelog" +
                " (\"effect_id\",\"changed_in_version_group_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"move_effect_changelog\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, MoveEffectChangelog entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getEffectId());
        stmt.bindLong(3, entity.getChangedInVersionGroupId());
    }

    @Override
    protected void attachEntity(MoveEffectChangelog entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public MoveEffectChangelog readEntity(Cursor cursor, int offset) {
        MoveEffectChangelog entity = new MoveEffectChangelog( //
            cursor.getLong(offset + 0), // Id
            cursor.getLong(offset + 1), // EffectId
            cursor.getLong(offset + 2) // ChangedInVersionGroupId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, MoveEffectChangelog entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setEffectId(cursor.getLong(offset + 1));
        entity.setChangedInVersionGroupId(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(MoveEffectChangelog entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(MoveEffectChangelog entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMoveEffectsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVersionGroupsDao().getAllColumns());
            builder.append(" FROM move_effect_changelog T");
            builder.append(" LEFT JOIN move_effects T0 ON T.\"effect_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN version_groups T1 ON T.\"changed_in_version_group_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected MoveEffectChangelog loadCurrentDeep(Cursor cursor, boolean lock) {
        MoveEffectChangelog entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        MoveEffects MoveEffects = loadCurrentOther(daoSession.getMoveEffectsDao(), cursor, offset);
         if(MoveEffects != null) {
            entity.setMoveEffects(MoveEffects);
        }
        offset += daoSession.getMoveEffectsDao().getAllColumns().length;

        VersionGroups VersionGroups = loadCurrentOther(daoSession.getVersionGroupsDao(), cursor, offset);
         if(VersionGroups != null) {
            entity.setVersionGroups(VersionGroups);
        }

        return entity;    
    }

    public MoveEffectChangelog loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<MoveEffectChangelog> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<MoveEffectChangelog> list = new ArrayList<MoveEffectChangelog>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<MoveEffectChangelog> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<MoveEffectChangelog> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
