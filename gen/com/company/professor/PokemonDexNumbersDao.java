package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonDexNumbers;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_dex_numbers".
*/
public class PokemonDexNumbersDao extends AbstractDao<PokemonDexNumbers, Long> {

    public static final String TABLENAME = "pokemon_dex_numbers";

    /**
     * Properties of entity PokemonDexNumbers.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property SpeciesId = new Property(0, long.class, "SpeciesId", true, "species_id");
        public final static Property PokedexId = new Property(1, long.class, "PokedexId", false, "pokedex_id");
        public final static Property PokedexNumber = new Property(2, long.class, "PokedexNumber", false, "pokedex_number");
    };

    private DaoSession daoSession;


    public PokemonDexNumbersDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonDexNumbersDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_dex_numbers\" (" + //
                "\"species_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: SpeciesId
                "\"pokedex_id\" INTEGER NOT NULL ," + // 1: PokedexId
                "\"pokedex_number\" INTEGER NOT NULL );"); // 2: PokedexNumber
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_dex_numbers_species_id_pokedex_id ON pokemon_dex_numbers" +
                " (\"species_id\",\"pokedex_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_dex_numbers\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonDexNumbers entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getSpeciesId());
        stmt.bindLong(2, entity.getPokedexId());
        stmt.bindLong(3, entity.getPokedexNumber());
    }

    @Override
    protected void attachEntity(PokemonDexNumbers entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonDexNumbers readEntity(Cursor cursor, int offset) {
        PokemonDexNumbers entity = new PokemonDexNumbers( //
            cursor.getLong(offset + 0), // SpeciesId
            cursor.getLong(offset + 1), // PokedexId
            cursor.getLong(offset + 2) // PokedexNumber
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonDexNumbers entity, int offset) {
        entity.setSpeciesId(cursor.getLong(offset + 0));
        entity.setPokedexId(cursor.getLong(offset + 1));
        entity.setPokedexNumber(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonDexNumbers entity, long rowId) {
        entity.setSpeciesId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonDexNumbers entity) {
        if(entity != null) {
            return entity.getSpeciesId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonSpeciesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPokedexesDao().getAllColumns());
            builder.append(" FROM pokemon_dex_numbers T");
            builder.append(" LEFT JOIN pokemon_species T0 ON T.\"species_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN pokedexes T1 ON T.\"pokedex_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonDexNumbers loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonDexNumbers entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PokemonSpecies PokemonSpecies = loadCurrentOther(daoSession.getPokemonSpeciesDao(), cursor, offset);
         if(PokemonSpecies != null) {
            entity.setPokemonSpecies(PokemonSpecies);
        }
        offset += daoSession.getPokemonSpeciesDao().getAllColumns().length;

        Pokedexes Pokedexes = loadCurrentOther(daoSession.getPokedexesDao(), cursor, offset);
         if(Pokedexes != null) {
            entity.setPokedexes(Pokedexes);
        }

        return entity;    
    }

    public PokemonDexNumbers loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonDexNumbers> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonDexNumbers> list = new ArrayList<PokemonDexNumbers>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonDexNumbers> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonDexNumbers> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
