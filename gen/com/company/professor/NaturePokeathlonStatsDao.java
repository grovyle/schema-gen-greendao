package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.NaturePokeathlonStats;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "nature_pokeathlon_stats".
*/
public class NaturePokeathlonStatsDao extends AbstractDao<NaturePokeathlonStats, Long> {

    public static final String TABLENAME = "nature_pokeathlon_stats";

    /**
     * Properties of entity NaturePokeathlonStats.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property NatureId = new Property(0, long.class, "NatureId", true, "nature_id");
        public final static Property PokeathlonStatId = new Property(1, long.class, "PokeathlonStatId", false, "pokeathlon_stat_id");
        public final static Property MaxChange = new Property(2, long.class, "MaxChange", false, "max_change");
    };

    private DaoSession daoSession;


    public NaturePokeathlonStatsDao(DaoConfig config) {
        super(config);
    }
    
    public NaturePokeathlonStatsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"nature_pokeathlon_stats\" (" + //
                "\"nature_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: NatureId
                "\"pokeathlon_stat_id\" INTEGER NOT NULL ," + // 1: PokeathlonStatId
                "\"max_change\" INTEGER NOT NULL );"); // 2: MaxChange
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_nature_pokeathlon_stats_nature_id_pokeathlon_stat_id ON nature_pokeathlon_stats" +
                " (\"nature_id\",\"pokeathlon_stat_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"nature_pokeathlon_stats\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, NaturePokeathlonStats entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getNatureId());
        stmt.bindLong(2, entity.getPokeathlonStatId());
        stmt.bindLong(3, entity.getMaxChange());
    }

    @Override
    protected void attachEntity(NaturePokeathlonStats entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public NaturePokeathlonStats readEntity(Cursor cursor, int offset) {
        NaturePokeathlonStats entity = new NaturePokeathlonStats( //
            cursor.getLong(offset + 0), // NatureId
            cursor.getLong(offset + 1), // PokeathlonStatId
            cursor.getLong(offset + 2) // MaxChange
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, NaturePokeathlonStats entity, int offset) {
        entity.setNatureId(cursor.getLong(offset + 0));
        entity.setPokeathlonStatId(cursor.getLong(offset + 1));
        entity.setMaxChange(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(NaturePokeathlonStats entity, long rowId) {
        entity.setNatureId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(NaturePokeathlonStats entity) {
        if(entity != null) {
            return entity.getNatureId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getNaturesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPokeathlonStatsDao().getAllColumns());
            builder.append(" FROM nature_pokeathlon_stats T");
            builder.append(" LEFT JOIN natures T0 ON T.\"nature_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN pokeathlon_stats T1 ON T.\"pokeathlon_stat_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected NaturePokeathlonStats loadCurrentDeep(Cursor cursor, boolean lock) {
        NaturePokeathlonStats entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Natures Natures = loadCurrentOther(daoSession.getNaturesDao(), cursor, offset);
         if(Natures != null) {
            entity.setNatures(Natures);
        }
        offset += daoSession.getNaturesDao().getAllColumns().length;

        PokeathlonStats PokeathlonStats = loadCurrentOther(daoSession.getPokeathlonStatsDao(), cursor, offset);
         if(PokeathlonStats != null) {
            entity.setPokeathlonStats(PokeathlonStats);
        }

        return entity;    
    }

    public NaturePokeathlonStats loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<NaturePokeathlonStats> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<NaturePokeathlonStats> list = new ArrayList<NaturePokeathlonStats>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<NaturePokeathlonStats> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<NaturePokeathlonStats> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
