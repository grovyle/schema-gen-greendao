package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonStats;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_stats".
*/
public class PokemonStatsDao extends AbstractDao<PokemonStats, Long> {

    public static final String TABLENAME = "pokemon_stats";

    /**
     * Properties of entity PokemonStats.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property PokemonId = new Property(0, long.class, "PokemonId", true, "pokemon_id");
        public final static Property StatId = new Property(1, long.class, "StatId", false, "stat_id");
        public final static Property BaseStat = new Property(2, long.class, "BaseStat", false, "base_stat");
        public final static Property Effort = new Property(3, long.class, "Effort", false, "effort");
    };

    private DaoSession daoSession;


    public PokemonStatsDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonStatsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_stats\" (" + //
                "\"pokemon_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: PokemonId
                "\"stat_id\" INTEGER NOT NULL ," + // 1: StatId
                "\"base_stat\" INTEGER NOT NULL ," + // 2: BaseStat
                "\"effort\" INTEGER NOT NULL );"); // 3: Effort
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_stats_pokemon_id_stat_id ON pokemon_stats" +
                " (\"pokemon_id\",\"stat_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_stats\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonStats entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getPokemonId());
        stmt.bindLong(2, entity.getStatId());
        stmt.bindLong(3, entity.getBaseStat());
        stmt.bindLong(4, entity.getEffort());
    }

    @Override
    protected void attachEntity(PokemonStats entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonStats readEntity(Cursor cursor, int offset) {
        PokemonStats entity = new PokemonStats( //
            cursor.getLong(offset + 0), // PokemonId
            cursor.getLong(offset + 1), // StatId
            cursor.getLong(offset + 2), // BaseStat
            cursor.getLong(offset + 3) // Effort
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonStats entity, int offset) {
        entity.setPokemonId(cursor.getLong(offset + 0));
        entity.setStatId(cursor.getLong(offset + 1));
        entity.setBaseStat(cursor.getLong(offset + 2));
        entity.setEffort(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonStats entity, long rowId) {
        entity.setPokemonId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonStats entity) {
        if(entity != null) {
            return entity.getPokemonId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getStatsDao().getAllColumns());
            builder.append(" FROM pokemon_stats T");
            builder.append(" LEFT JOIN pokemon T0 ON T.\"pokemon_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN stats T1 ON T.\"stat_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonStats loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonStats entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Pokemon Pokemon = loadCurrentOther(daoSession.getPokemonDao(), cursor, offset);
         if(Pokemon != null) {
            entity.setPokemon(Pokemon);
        }
        offset += daoSession.getPokemonDao().getAllColumns().length;

        Stats Stats = loadCurrentOther(daoSession.getStatsDao(), cursor, offset);
         if(Stats != null) {
            entity.setStats(Stats);
        }

        return entity;    
    }

    public PokemonStats loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonStats> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonStats> list = new ArrayList<PokemonStats>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonStats> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonStats> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
