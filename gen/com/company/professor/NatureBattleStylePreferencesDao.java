package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.NatureBattleStylePreferences;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "nature_battle_style_preferences".
*/
public class NatureBattleStylePreferencesDao extends AbstractDao<NatureBattleStylePreferences, Long> {

    public static final String TABLENAME = "nature_battle_style_preferences";

    /**
     * Properties of entity NatureBattleStylePreferences.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property NatureId = new Property(0, long.class, "NatureId", true, "nature_id");
        public final static Property MoveBattleStyleId = new Property(1, long.class, "MoveBattleStyleId", false, "move_battle_style_id");
        public final static Property LowHpPreference = new Property(2, long.class, "LowHpPreference", false, "low_hp_preference");
        public final static Property HighHpPreference = new Property(3, long.class, "HighHpPreference", false, "high_hp_preference");
    };

    private DaoSession daoSession;


    public NatureBattleStylePreferencesDao(DaoConfig config) {
        super(config);
    }
    
    public NatureBattleStylePreferencesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"nature_battle_style_preferences\" (" + //
                "\"nature_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: NatureId
                "\"move_battle_style_id\" INTEGER NOT NULL ," + // 1: MoveBattleStyleId
                "\"low_hp_preference\" INTEGER NOT NULL ," + // 2: LowHpPreference
                "\"high_hp_preference\" INTEGER NOT NULL );"); // 3: HighHpPreference
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_nature_battle_style_preferences_nature_id_move_battle_style_id ON nature_battle_style_preferences" +
                " (\"nature_id\",\"move_battle_style_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"nature_battle_style_preferences\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, NatureBattleStylePreferences entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getNatureId());
        stmt.bindLong(2, entity.getMoveBattleStyleId());
        stmt.bindLong(3, entity.getLowHpPreference());
        stmt.bindLong(4, entity.getHighHpPreference());
    }

    @Override
    protected void attachEntity(NatureBattleStylePreferences entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public NatureBattleStylePreferences readEntity(Cursor cursor, int offset) {
        NatureBattleStylePreferences entity = new NatureBattleStylePreferences( //
            cursor.getLong(offset + 0), // NatureId
            cursor.getLong(offset + 1), // MoveBattleStyleId
            cursor.getLong(offset + 2), // LowHpPreference
            cursor.getLong(offset + 3) // HighHpPreference
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, NatureBattleStylePreferences entity, int offset) {
        entity.setNatureId(cursor.getLong(offset + 0));
        entity.setMoveBattleStyleId(cursor.getLong(offset + 1));
        entity.setLowHpPreference(cursor.getLong(offset + 2));
        entity.setHighHpPreference(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(NatureBattleStylePreferences entity, long rowId) {
        entity.setNatureId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(NatureBattleStylePreferences entity) {
        if(entity != null) {
            return entity.getNatureId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getNaturesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getMoveBattleStylesDao().getAllColumns());
            builder.append(" FROM nature_battle_style_preferences T");
            builder.append(" LEFT JOIN natures T0 ON T.\"nature_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN move_battle_styles T1 ON T.\"move_battle_style_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected NatureBattleStylePreferences loadCurrentDeep(Cursor cursor, boolean lock) {
        NatureBattleStylePreferences entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Natures Natures = loadCurrentOther(daoSession.getNaturesDao(), cursor, offset);
         if(Natures != null) {
            entity.setNatures(Natures);
        }
        offset += daoSession.getNaturesDao().getAllColumns().length;

        MoveBattleStyles MoveBattleStyles = loadCurrentOther(daoSession.getMoveBattleStylesDao(), cursor, offset);
         if(MoveBattleStyles != null) {
            entity.setMoveBattleStyles(MoveBattleStyles);
        }

        return entity;    
    }

    public NatureBattleStylePreferences loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<NatureBattleStylePreferences> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<NatureBattleStylePreferences> list = new ArrayList<NatureBattleStylePreferences>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<NatureBattleStylePreferences> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<NatureBattleStylePreferences> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
