package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokedexVersionGroups;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokedex_version_groups".
*/
public class PokedexVersionGroupsDao extends AbstractDao<PokedexVersionGroups, Long> {

    public static final String TABLENAME = "pokedex_version_groups";

    /**
     * Properties of entity PokedexVersionGroups.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property PokedexId = new Property(0, long.class, "PokedexId", true, "pokedex_id");
        public final static Property VersionGroupId = new Property(1, long.class, "VersionGroupId", false, "version_group_id");
    };

    private DaoSession daoSession;


    public PokedexVersionGroupsDao(DaoConfig config) {
        super(config);
    }
    
    public PokedexVersionGroupsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokedex_version_groups\" (" + //
                "\"pokedex_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: PokedexId
                "\"version_group_id\" INTEGER NOT NULL );"); // 1: VersionGroupId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokedex_version_groups_pokedex_id_version_group_id ON pokedex_version_groups" +
                " (\"pokedex_id\",\"version_group_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokedex_version_groups\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokedexVersionGroups entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getPokedexId());
        stmt.bindLong(2, entity.getVersionGroupId());
    }

    @Override
    protected void attachEntity(PokedexVersionGroups entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokedexVersionGroups readEntity(Cursor cursor, int offset) {
        PokedexVersionGroups entity = new PokedexVersionGroups( //
            cursor.getLong(offset + 0), // PokedexId
            cursor.getLong(offset + 1) // VersionGroupId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokedexVersionGroups entity, int offset) {
        entity.setPokedexId(cursor.getLong(offset + 0));
        entity.setVersionGroupId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokedexVersionGroups entity, long rowId) {
        entity.setPokedexId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokedexVersionGroups entity) {
        if(entity != null) {
            return entity.getPokedexId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokedexesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVersionGroupsDao().getAllColumns());
            builder.append(" FROM pokedex_version_groups T");
            builder.append(" LEFT JOIN pokedexes T0 ON T.\"pokedex_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN version_groups T1 ON T.\"version_group_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokedexVersionGroups loadCurrentDeep(Cursor cursor, boolean lock) {
        PokedexVersionGroups entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Pokedexes Pokedexes = loadCurrentOther(daoSession.getPokedexesDao(), cursor, offset);
         if(Pokedexes != null) {
            entity.setPokedexes(Pokedexes);
        }
        offset += daoSession.getPokedexesDao().getAllColumns().length;

        VersionGroups VersionGroups = loadCurrentOther(daoSession.getVersionGroupsDao(), cursor, offset);
         if(VersionGroups != null) {
            entity.setVersionGroups(VersionGroups);
        }

        return entity;    
    }

    public PokedexVersionGroups loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokedexVersionGroups> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokedexVersionGroups> list = new ArrayList<PokedexVersionGroups>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokedexVersionGroups> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokedexVersionGroups> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
