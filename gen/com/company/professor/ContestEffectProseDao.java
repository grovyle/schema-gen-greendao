package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ContestEffectProse;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "contest_effect_prose".
*/
public class ContestEffectProseDao extends AbstractDao<ContestEffectProse, Long> {

    public static final String TABLENAME = "contest_effect_prose";

    /**
     * Properties of entity ContestEffectProse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property ContestEffectId = new Property(0, long.class, "ContestEffectId", true, "contest_effect_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property FlavorText = new Property(2, String.class, "FlavorText", false, "flavor_text");
        public final static Property Effect = new Property(3, String.class, "Effect", false, "effect");
    };

    private DaoSession daoSession;


    public ContestEffectProseDao(DaoConfig config) {
        super(config);
    }
    
    public ContestEffectProseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"contest_effect_prose\" (" + //
                "\"contest_effect_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: ContestEffectId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"flavor_text\" TEXT," + // 2: FlavorText
                "\"effect\" TEXT);"); // 3: Effect
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_contest_effect_prose_contest_effect_id_local_language_id ON contest_effect_prose" +
                " (\"contest_effect_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"contest_effect_prose\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ContestEffectProse entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getContestEffectId());
        stmt.bindLong(2, entity.getLocalLanguageId());
 
        String FlavorText = entity.getFlavorText();
        if (FlavorText != null) {
            stmt.bindString(3, FlavorText);
        }
 
        String Effect = entity.getEffect();
        if (Effect != null) {
            stmt.bindString(4, Effect);
        }
    }

    @Override
    protected void attachEntity(ContestEffectProse entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ContestEffectProse readEntity(Cursor cursor, int offset) {
        ContestEffectProse entity = new ContestEffectProse( //
            cursor.getLong(offset + 0), // ContestEffectId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FlavorText
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // Effect
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ContestEffectProse entity, int offset) {
        entity.setContestEffectId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setFlavorText(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setEffect(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ContestEffectProse entity, long rowId) {
        entity.setContestEffectId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ContestEffectProse entity) {
        if(entity != null) {
            return entity.getContestEffectId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getContestEffectsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM contest_effect_prose T");
            builder.append(" LEFT JOIN contest_effects T0 ON T.\"contest_effect_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ContestEffectProse loadCurrentDeep(Cursor cursor, boolean lock) {
        ContestEffectProse entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ContestEffects ContestEffects = loadCurrentOther(daoSession.getContestEffectsDao(), cursor, offset);
         if(ContestEffects != null) {
            entity.setContestEffects(ContestEffects);
        }
        offset += daoSession.getContestEffectsDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public ContestEffectProse loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ContestEffectProse> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ContestEffectProse> list = new ArrayList<ContestEffectProse>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ContestEffectProse> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ContestEffectProse> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
