package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.MoveEffectChangelogProse;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "move_effect_changelog_prose".
*/
public class MoveEffectChangelogProseDao extends AbstractDao<MoveEffectChangelogProse, Long> {

    public static final String TABLENAME = "move_effect_changelog_prose";

    /**
     * Properties of entity MoveEffectChangelogProse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property MoveEffectChangelogId = new Property(0, long.class, "MoveEffectChangelogId", true, "move_effect_changelog_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property Effect = new Property(2, String.class, "Effect", false, "effect");
    };

    private DaoSession daoSession;


    public MoveEffectChangelogProseDao(DaoConfig config) {
        super(config);
    }
    
    public MoveEffectChangelogProseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"move_effect_changelog_prose\" (" + //
                "\"move_effect_changelog_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: MoveEffectChangelogId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"effect\" TEXT NOT NULL );"); // 2: Effect
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_move_effect_changelog_prose_move_effect_changelog_id_local_language_id ON move_effect_changelog_prose" +
                " (\"move_effect_changelog_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"move_effect_changelog_prose\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, MoveEffectChangelogProse entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getMoveEffectChangelogId());
        stmt.bindLong(2, entity.getLocalLanguageId());
        stmt.bindString(3, entity.getEffect());
    }

    @Override
    protected void attachEntity(MoveEffectChangelogProse entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public MoveEffectChangelogProse readEntity(Cursor cursor, int offset) {
        MoveEffectChangelogProse entity = new MoveEffectChangelogProse( //
            cursor.getLong(offset + 0), // MoveEffectChangelogId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.getString(offset + 2) // Effect
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, MoveEffectChangelogProse entity, int offset) {
        entity.setMoveEffectChangelogId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setEffect(cursor.getString(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(MoveEffectChangelogProse entity, long rowId) {
        entity.setMoveEffectChangelogId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(MoveEffectChangelogProse entity) {
        if(entity != null) {
            return entity.getMoveEffectChangelogId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMoveEffectChangelogDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM move_effect_changelog_prose T");
            builder.append(" LEFT JOIN move_effect_changelog T0 ON T.\"move_effect_changelog_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected MoveEffectChangelogProse loadCurrentDeep(Cursor cursor, boolean lock) {
        MoveEffectChangelogProse entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        MoveEffectChangelog MoveEffectChangelog = loadCurrentOther(daoSession.getMoveEffectChangelogDao(), cursor, offset);
         if(MoveEffectChangelog != null) {
            entity.setMoveEffectChangelog(MoveEffectChangelog);
        }
        offset += daoSession.getMoveEffectChangelogDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public MoveEffectChangelogProse loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<MoveEffectChangelogProse> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<MoveEffectChangelogProse> list = new ArrayList<MoveEffectChangelogProse>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<MoveEffectChangelogProse> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<MoveEffectChangelogProse> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
