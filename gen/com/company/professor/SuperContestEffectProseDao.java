package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.SuperContestEffectProse;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "super_contest_effect_prose".
*/
public class SuperContestEffectProseDao extends AbstractDao<SuperContestEffectProse, Long> {

    public static final String TABLENAME = "super_contest_effect_prose";

    /**
     * Properties of entity SuperContestEffectProse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property SuperContestEffectId = new Property(0, long.class, "SuperContestEffectId", true, "super_contest_effect_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property FlavorText = new Property(2, String.class, "FlavorText", false, "flavor_text");
    };

    private DaoSession daoSession;


    public SuperContestEffectProseDao(DaoConfig config) {
        super(config);
    }
    
    public SuperContestEffectProseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"super_contest_effect_prose\" (" + //
                "\"super_contest_effect_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: SuperContestEffectId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"flavor_text\" TEXT NOT NULL );"); // 2: FlavorText
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_super_contest_effect_prose_super_contest_effect_id_local_language_id ON super_contest_effect_prose" +
                " (\"super_contest_effect_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"super_contest_effect_prose\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SuperContestEffectProse entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getSuperContestEffectId());
        stmt.bindLong(2, entity.getLocalLanguageId());
        stmt.bindString(3, entity.getFlavorText());
    }

    @Override
    protected void attachEntity(SuperContestEffectProse entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SuperContestEffectProse readEntity(Cursor cursor, int offset) {
        SuperContestEffectProse entity = new SuperContestEffectProse( //
            cursor.getLong(offset + 0), // SuperContestEffectId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.getString(offset + 2) // FlavorText
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SuperContestEffectProse entity, int offset) {
        entity.setSuperContestEffectId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setFlavorText(cursor.getString(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SuperContestEffectProse entity, long rowId) {
        entity.setSuperContestEffectId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SuperContestEffectProse entity) {
        if(entity != null) {
            return entity.getSuperContestEffectId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getSuperContestEffectsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM super_contest_effect_prose T");
            builder.append(" LEFT JOIN super_contest_effects T0 ON T.\"super_contest_effect_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected SuperContestEffectProse loadCurrentDeep(Cursor cursor, boolean lock) {
        SuperContestEffectProse entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        SuperContestEffects SuperContestEffects = loadCurrentOther(daoSession.getSuperContestEffectsDao(), cursor, offset);
         if(SuperContestEffects != null) {
            entity.setSuperContestEffects(SuperContestEffects);
        }
        offset += daoSession.getSuperContestEffectsDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public SuperContestEffectProse loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<SuperContestEffectProse> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<SuperContestEffectProse> list = new ArrayList<SuperContestEffectProse>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<SuperContestEffectProse> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<SuperContestEffectProse> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
