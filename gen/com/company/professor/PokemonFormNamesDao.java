package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonFormNames;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_form_names".
*/
public class PokemonFormNamesDao extends AbstractDao<PokemonFormNames, Long> {

    public static final String TABLENAME = "pokemon_form_names";

    /**
     * Properties of entity PokemonFormNames.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property PokemonFormId = new Property(0, long.class, "PokemonFormId", true, "pokemon_form_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property FormName = new Property(2, String.class, "FormName", false, "form_name");
        public final static Property PokemonName = new Property(3, String.class, "PokemonName", false, "pokemon_name");
    };

    private DaoSession daoSession;


    public PokemonFormNamesDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonFormNamesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_form_names\" (" + //
                "\"pokemon_form_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: PokemonFormId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"form_name\" TEXT," + // 2: FormName
                "\"pokemon_name\" TEXT);"); // 3: PokemonName
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_form_names_pokemon_form_id_local_language_id ON pokemon_form_names" +
                " (\"pokemon_form_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_form_names\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonFormNames entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getPokemonFormId());
        stmt.bindLong(2, entity.getLocalLanguageId());
 
        String FormName = entity.getFormName();
        if (FormName != null) {
            stmt.bindString(3, FormName);
        }
 
        String PokemonName = entity.getPokemonName();
        if (PokemonName != null) {
            stmt.bindString(4, PokemonName);
        }
    }

    @Override
    protected void attachEntity(PokemonFormNames entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonFormNames readEntity(Cursor cursor, int offset) {
        PokemonFormNames entity = new PokemonFormNames( //
            cursor.getLong(offset + 0), // PokemonFormId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FormName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // PokemonName
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonFormNames entity, int offset) {
        entity.setPokemonFormId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setFormName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPokemonName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonFormNames entity, long rowId) {
        entity.setPokemonFormId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonFormNames entity) {
        if(entity != null) {
            return entity.getPokemonFormId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonFormsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM pokemon_form_names T");
            builder.append(" LEFT JOIN pokemon_forms T0 ON T.\"pokemon_form_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonFormNames loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonFormNames entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PokemonForms PokemonForms = loadCurrentOther(daoSession.getPokemonFormsDao(), cursor, offset);
         if(PokemonForms != null) {
            entity.setPokemonForms(PokemonForms);
        }
        offset += daoSession.getPokemonFormsDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public PokemonFormNames loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonFormNames> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonFormNames> list = new ArrayList<PokemonFormNames>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonFormNames> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonFormNames> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
