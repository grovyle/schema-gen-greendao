package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.AbilityProse;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ability_prose".
*/
public class AbilityProseDao extends AbstractDao<AbilityProse, Long> {

    public static final String TABLENAME = "ability_prose";

    /**
     * Properties of entity AbilityProse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property AbilityId = new Property(0, long.class, "AbilityId", true, "ability_id");
        public final static Property LocalLanguageId = new Property(1, long.class, "LocalLanguageId", false, "local_language_id");
        public final static Property ShortEffect = new Property(2, String.class, "ShortEffect", false, "short_effect");
        public final static Property Effect = new Property(3, String.class, "Effect", false, "effect");
    };

    private DaoSession daoSession;


    public AbilityProseDao(DaoConfig config) {
        super(config);
    }
    
    public AbilityProseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ability_prose\" (" + //
                "\"ability_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: AbilityId
                "\"local_language_id\" INTEGER NOT NULL ," + // 1: LocalLanguageId
                "\"short_effect\" TEXT," + // 2: ShortEffect
                "\"effect\" TEXT);"); // 3: Effect
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_ability_prose_ability_id_local_language_id ON ability_prose" +
                " (\"ability_id\",\"local_language_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ability_prose\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, AbilityProse entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getAbilityId());
        stmt.bindLong(2, entity.getLocalLanguageId());
 
        String ShortEffect = entity.getShortEffect();
        if (ShortEffect != null) {
            stmt.bindString(3, ShortEffect);
        }
 
        String Effect = entity.getEffect();
        if (Effect != null) {
            stmt.bindString(4, Effect);
        }
    }

    @Override
    protected void attachEntity(AbilityProse entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public AbilityProse readEntity(Cursor cursor, int offset) {
        AbilityProse entity = new AbilityProse( //
            cursor.getLong(offset + 0), // AbilityId
            cursor.getLong(offset + 1), // LocalLanguageId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // ShortEffect
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // Effect
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, AbilityProse entity, int offset) {
        entity.setAbilityId(cursor.getLong(offset + 0));
        entity.setLocalLanguageId(cursor.getLong(offset + 1));
        entity.setShortEffect(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setEffect(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(AbilityProse entity, long rowId) {
        entity.setAbilityId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(AbilityProse entity) {
        if(entity != null) {
            return entity.getAbilityId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAbilitiesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getLanguagesDao().getAllColumns());
            builder.append(" FROM ability_prose T");
            builder.append(" LEFT JOIN abilities T0 ON T.\"ability_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN languages T1 ON T.\"local_language_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected AbilityProse loadCurrentDeep(Cursor cursor, boolean lock) {
        AbilityProse entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Abilities Abilities = loadCurrentOther(daoSession.getAbilitiesDao(), cursor, offset);
         if(Abilities != null) {
            entity.setAbilities(Abilities);
        }
        offset += daoSession.getAbilitiesDao().getAllColumns().length;

        Languages Languages = loadCurrentOther(daoSession.getLanguagesDao(), cursor, offset);
         if(Languages != null) {
            entity.setLanguages(Languages);
        }

        return entity;    
    }

    public AbilityProse loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<AbilityProse> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<AbilityProse> list = new ArrayList<AbilityProse>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<AbilityProse> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<AbilityProse> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
