package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.TypeEfficacy;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "type_efficacy".
*/
public class TypeEfficacyDao extends AbstractDao<TypeEfficacy, Long> {

    public static final String TABLENAME = "type_efficacy";

    /**
     * Properties of entity TypeEfficacy.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property DamageTypeId = new Property(0, long.class, "DamageTypeId", true, "damage_type_id");
        public final static Property TargetTypeId = new Property(1, long.class, "TargetTypeId", false, "target_type_id");
        public final static Property DamageFactor = new Property(2, long.class, "DamageFactor", false, "damage_factor");
    };

    private DaoSession daoSession;


    public TypeEfficacyDao(DaoConfig config) {
        super(config);
    }
    
    public TypeEfficacyDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"type_efficacy\" (" + //
                "\"damage_type_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: DamageTypeId
                "\"target_type_id\" INTEGER NOT NULL ," + // 1: TargetTypeId
                "\"damage_factor\" INTEGER NOT NULL );"); // 2: DamageFactor
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_type_efficacy_damage_type_id_target_type_id ON type_efficacy" +
                " (\"damage_type_id\",\"target_type_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"type_efficacy\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TypeEfficacy entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getDamageTypeId());
        stmt.bindLong(2, entity.getTargetTypeId());
        stmt.bindLong(3, entity.getDamageFactor());
    }

    @Override
    protected void attachEntity(TypeEfficacy entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TypeEfficacy readEntity(Cursor cursor, int offset) {
        TypeEfficacy entity = new TypeEfficacy( //
            cursor.getLong(offset + 0), // DamageTypeId
            cursor.getLong(offset + 1), // TargetTypeId
            cursor.getLong(offset + 2) // DamageFactor
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TypeEfficacy entity, int offset) {
        entity.setDamageTypeId(cursor.getLong(offset + 0));
        entity.setTargetTypeId(cursor.getLong(offset + 1));
        entity.setDamageFactor(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TypeEfficacy entity, long rowId) {
        entity.setDamageTypeId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TypeEfficacy entity) {
        if(entity != null) {
            return entity.getDamageTypeId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTypesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getTypesDao().getAllColumns());
            builder.append(" FROM type_efficacy T");
            builder.append(" LEFT JOIN types T0 ON T.\"damage_type_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN types T1 ON T.\"target_type_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TypeEfficacy loadCurrentDeep(Cursor cursor, boolean lock) {
        TypeEfficacy entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Types DamageType = loadCurrentOther(daoSession.getTypesDao(), cursor, offset);
         if(DamageType != null) {
            entity.setDamageType(DamageType);
        }
        offset += daoSession.getTypesDao().getAllColumns().length;

        Types TargetType = loadCurrentOther(daoSession.getTypesDao(), cursor, offset);
         if(TargetType != null) {
            entity.setTargetType(TargetType);
        }

        return entity;    
    }

    public TypeEfficacy loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TypeEfficacy> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TypeEfficacy> list = new ArrayList<TypeEfficacy>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TypeEfficacy> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TypeEfficacy> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
