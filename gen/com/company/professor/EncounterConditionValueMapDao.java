package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.EncounterConditionValueMap;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "encounter_condition_value_map".
*/
public class EncounterConditionValueMapDao extends AbstractDao<EncounterConditionValueMap, Long> {

    public static final String TABLENAME = "encounter_condition_value_map";

    /**
     * Properties of entity EncounterConditionValueMap.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property EncounterId = new Property(0, long.class, "EncounterId", true, "encounter_id");
        public final static Property EncounterConditionValueId = new Property(1, long.class, "EncounterConditionValueId", false, "encounter_condition_value_id");
    };

    private DaoSession daoSession;


    public EncounterConditionValueMapDao(DaoConfig config) {
        super(config);
    }
    
    public EncounterConditionValueMapDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"encounter_condition_value_map\" (" + //
                "\"encounter_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: EncounterId
                "\"encounter_condition_value_id\" INTEGER NOT NULL );"); // 1: EncounterConditionValueId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_encounter_condition_value_map_encounter_id_encounter_condition_value_id ON encounter_condition_value_map" +
                " (\"encounter_id\",\"encounter_condition_value_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"encounter_condition_value_map\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, EncounterConditionValueMap entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getEncounterId());
        stmt.bindLong(2, entity.getEncounterConditionValueId());
    }

    @Override
    protected void attachEntity(EncounterConditionValueMap entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public EncounterConditionValueMap readEntity(Cursor cursor, int offset) {
        EncounterConditionValueMap entity = new EncounterConditionValueMap( //
            cursor.getLong(offset + 0), // EncounterId
            cursor.getLong(offset + 1) // EncounterConditionValueId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, EncounterConditionValueMap entity, int offset) {
        entity.setEncounterId(cursor.getLong(offset + 0));
        entity.setEncounterConditionValueId(cursor.getLong(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(EncounterConditionValueMap entity, long rowId) {
        entity.setEncounterId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(EncounterConditionValueMap entity) {
        if(entity != null) {
            return entity.getEncounterId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getEncountersDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getEncounterConditionValuesDao().getAllColumns());
            builder.append(" FROM encounter_condition_value_map T");
            builder.append(" LEFT JOIN encounters T0 ON T.\"encounter_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN encounter_condition_values T1 ON T.\"encounter_condition_value_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected EncounterConditionValueMap loadCurrentDeep(Cursor cursor, boolean lock) {
        EncounterConditionValueMap entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Encounters Encounters = loadCurrentOther(daoSession.getEncountersDao(), cursor, offset);
         if(Encounters != null) {
            entity.setEncounters(Encounters);
        }
        offset += daoSession.getEncountersDao().getAllColumns().length;

        EncounterConditionValues EncounterConditionValues = loadCurrentOther(daoSession.getEncounterConditionValuesDao(), cursor, offset);
         if(EncounterConditionValues != null) {
            entity.setEncounterConditionValues(EncounterConditionValues);
        }

        return entity;    
    }

    public EncounterConditionValueMap loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<EncounterConditionValueMap> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<EncounterConditionValueMap> list = new ArrayList<EncounterConditionValueMap>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<EncounterConditionValueMap> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<EncounterConditionValueMap> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
