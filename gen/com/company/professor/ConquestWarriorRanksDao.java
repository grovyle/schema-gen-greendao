package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ConquestWarriorRanks;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "conquest_warrior_ranks".
*/
public class ConquestWarriorRanksDao extends AbstractDao<ConquestWarriorRanks, Long> {

    public static final String TABLENAME = "conquest_warrior_ranks";

    /**
     * Properties of entity ConquestWarriorRanks.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property WarriorId = new Property(1, long.class, "WarriorId", false, "warrior_id");
        public final static Property Rank = new Property(2, long.class, "Rank", false, "rank");
        public final static Property SkillId = new Property(3, long.class, "SkillId", false, "skill_id");
    };

    private DaoSession daoSession;


    public ConquestWarriorRanksDao(DaoConfig config) {
        super(config);
    }
    
    public ConquestWarriorRanksDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"conquest_warrior_ranks\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"warrior_id\" INTEGER NOT NULL ," + // 1: WarriorId
                "\"rank\" INTEGER NOT NULL ," + // 2: Rank
                "\"skill_id\" INTEGER NOT NULL );"); // 3: SkillId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_conquest_warrior_ranks_warrior_id_rank ON conquest_warrior_ranks" +
                " (\"warrior_id\",\"rank\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"conquest_warrior_ranks\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConquestWarriorRanks entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getWarriorId());
        stmt.bindLong(3, entity.getRank());
        stmt.bindLong(4, entity.getSkillId());
    }

    @Override
    protected void attachEntity(ConquestWarriorRanks entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ConquestWarriorRanks readEntity(Cursor cursor, int offset) {
        ConquestWarriorRanks entity = new ConquestWarriorRanks( //
            cursor.getLong(offset + 0), // Id
            cursor.getLong(offset + 1), // WarriorId
            cursor.getLong(offset + 2), // Rank
            cursor.getLong(offset + 3) // SkillId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConquestWarriorRanks entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setWarriorId(cursor.getLong(offset + 1));
        entity.setRank(cursor.getLong(offset + 2));
        entity.setSkillId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConquestWarriorRanks entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ConquestWarriorRanks entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getConquestWarriorsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getConquestWarriorSkillsDao().getAllColumns());
            builder.append(" FROM conquest_warrior_ranks T");
            builder.append(" LEFT JOIN conquest_warriors T0 ON T.\"warrior_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN conquest_warrior_skills T1 ON T.\"skill_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ConquestWarriorRanks loadCurrentDeep(Cursor cursor, boolean lock) {
        ConquestWarriorRanks entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ConquestWarriors ConquestWarriors = loadCurrentOther(daoSession.getConquestWarriorsDao(), cursor, offset);
         if(ConquestWarriors != null) {
            entity.setConquestWarriors(ConquestWarriors);
        }
        offset += daoSession.getConquestWarriorsDao().getAllColumns().length;

        ConquestWarriorSkills ConquestWarriorSkills = loadCurrentOther(daoSession.getConquestWarriorSkillsDao(), cursor, offset);
         if(ConquestWarriorSkills != null) {
            entity.setConquestWarriorSkills(ConquestWarriorSkills);
        }

        return entity;    
    }

    public ConquestWarriorRanks loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ConquestWarriorRanks> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ConquestWarriorRanks> list = new ArrayList<ConquestWarriorRanks>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ConquestWarriorRanks> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ConquestWarriorRanks> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
