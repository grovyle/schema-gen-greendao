package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.PokemonFormPokeathlonStats;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "pokemon_form_pokeathlon_stats".
*/
public class PokemonFormPokeathlonStatsDao extends AbstractDao<PokemonFormPokeathlonStats, Long> {

    public static final String TABLENAME = "pokemon_form_pokeathlon_stats";

    /**
     * Properties of entity PokemonFormPokeathlonStats.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property PokemonFormId = new Property(0, long.class, "PokemonFormId", true, "pokemon_form_id");
        public final static Property PokeathlonStatId = new Property(1, long.class, "PokeathlonStatId", false, "pokeathlon_stat_id");
        public final static Property MinimumStat = new Property(2, long.class, "MinimumStat", false, "minimum_stat");
        public final static Property BaseStat = new Property(3, long.class, "BaseStat", false, "base_stat");
        public final static Property MaximumStat = new Property(4, long.class, "MaximumStat", false, "maximum_stat");
    };

    private DaoSession daoSession;


    public PokemonFormPokeathlonStatsDao(DaoConfig config) {
        super(config);
    }
    
    public PokemonFormPokeathlonStatsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"pokemon_form_pokeathlon_stats\" (" + //
                "\"pokemon_form_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: PokemonFormId
                "\"pokeathlon_stat_id\" INTEGER NOT NULL ," + // 1: PokeathlonStatId
                "\"minimum_stat\" INTEGER NOT NULL ," + // 2: MinimumStat
                "\"base_stat\" INTEGER NOT NULL ," + // 3: BaseStat
                "\"maximum_stat\" INTEGER NOT NULL );"); // 4: MaximumStat
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_pokemon_form_pokeathlon_stats_pokemon_form_id_pokeathlon_stat_id ON pokemon_form_pokeathlon_stats" +
                " (\"pokemon_form_id\",\"pokeathlon_stat_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"pokemon_form_pokeathlon_stats\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PokemonFormPokeathlonStats entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getPokemonFormId());
        stmt.bindLong(2, entity.getPokeathlonStatId());
        stmt.bindLong(3, entity.getMinimumStat());
        stmt.bindLong(4, entity.getBaseStat());
        stmt.bindLong(5, entity.getMaximumStat());
    }

    @Override
    protected void attachEntity(PokemonFormPokeathlonStats entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PokemonFormPokeathlonStats readEntity(Cursor cursor, int offset) {
        PokemonFormPokeathlonStats entity = new PokemonFormPokeathlonStats( //
            cursor.getLong(offset + 0), // PokemonFormId
            cursor.getLong(offset + 1), // PokeathlonStatId
            cursor.getLong(offset + 2), // MinimumStat
            cursor.getLong(offset + 3), // BaseStat
            cursor.getLong(offset + 4) // MaximumStat
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PokemonFormPokeathlonStats entity, int offset) {
        entity.setPokemonFormId(cursor.getLong(offset + 0));
        entity.setPokeathlonStatId(cursor.getLong(offset + 1));
        entity.setMinimumStat(cursor.getLong(offset + 2));
        entity.setBaseStat(cursor.getLong(offset + 3));
        entity.setMaximumStat(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PokemonFormPokeathlonStats entity, long rowId) {
        entity.setPokemonFormId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PokemonFormPokeathlonStats entity) {
        if(entity != null) {
            return entity.getPokemonFormId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPokemonFormsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPokeathlonStatsDao().getAllColumns());
            builder.append(" FROM pokemon_form_pokeathlon_stats T");
            builder.append(" LEFT JOIN pokemon_forms T0 ON T.\"pokemon_form_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN pokeathlon_stats T1 ON T.\"pokeathlon_stat_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PokemonFormPokeathlonStats loadCurrentDeep(Cursor cursor, boolean lock) {
        PokemonFormPokeathlonStats entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PokemonForms PokemonForms = loadCurrentOther(daoSession.getPokemonFormsDao(), cursor, offset);
         if(PokemonForms != null) {
            entity.setPokemonForms(PokemonForms);
        }
        offset += daoSession.getPokemonFormsDao().getAllColumns().length;

        PokeathlonStats PokeathlonStats = loadCurrentOther(daoSession.getPokeathlonStatsDao(), cursor, offset);
         if(PokeathlonStats != null) {
            entity.setPokeathlonStats(PokeathlonStats);
        }

        return entity;    
    }

    public PokemonFormPokeathlonStats loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PokemonFormPokeathlonStats> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PokemonFormPokeathlonStats> list = new ArrayList<PokemonFormPokeathlonStats>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PokemonFormPokeathlonStats> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PokemonFormPokeathlonStats> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
