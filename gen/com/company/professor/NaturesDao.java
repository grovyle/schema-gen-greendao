package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.Natures;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "natures".
*/
public class NaturesDao extends AbstractDao<Natures, Long> {

    public static final String TABLENAME = "natures";

    /**
     * Properties of entity Natures.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "Id", true, "id");
        public final static Property Identifier = new Property(1, String.class, "Identifier", false, "identifier");
        public final static Property DecreasedStatId = new Property(2, long.class, "DecreasedStatId", false, "decreased_stat_id");
        public final static Property IncreasedStatId = new Property(3, long.class, "IncreasedStatId", false, "increased_stat_id");
        public final static Property HatesFlavorId = new Property(4, long.class, "HatesFlavorId", false, "hates_flavor_id");
        public final static Property LikesFlavorId = new Property(5, long.class, "LikesFlavorId", false, "likes_flavor_id");
        public final static Property GameIndex = new Property(6, long.class, "GameIndex", false, "game_index");
    };

    private DaoSession daoSession;


    public NaturesDao(DaoConfig config) {
        super(config);
    }
    
    public NaturesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"natures\" (" + //
                "\"id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: Id
                "\"identifier\" TEXT NOT NULL ," + // 1: Identifier
                "\"decreased_stat_id\" INTEGER NOT NULL ," + // 2: DecreasedStatId
                "\"increased_stat_id\" INTEGER NOT NULL ," + // 3: IncreasedStatId
                "\"hates_flavor_id\" INTEGER NOT NULL ," + // 4: HatesFlavorId
                "\"likes_flavor_id\" INTEGER NOT NULL ," + // 5: LikesFlavorId
                "\"game_index\" INTEGER NOT NULL );"); // 6: GameIndex
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_natures_game_index ON natures" +
                " (\"game_index\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"natures\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Natures entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindString(2, entity.getIdentifier());
        stmt.bindLong(3, entity.getDecreasedStatId());
        stmt.bindLong(4, entity.getIncreasedStatId());
        stmt.bindLong(5, entity.getHatesFlavorId());
        stmt.bindLong(6, entity.getLikesFlavorId());
        stmt.bindLong(7, entity.getGameIndex());
    }

    @Override
    protected void attachEntity(Natures entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Natures readEntity(Cursor cursor, int offset) {
        Natures entity = new Natures( //
            cursor.getLong(offset + 0), // Id
            cursor.getString(offset + 1), // Identifier
            cursor.getLong(offset + 2), // DecreasedStatId
            cursor.getLong(offset + 3), // IncreasedStatId
            cursor.getLong(offset + 4), // HatesFlavorId
            cursor.getLong(offset + 5), // LikesFlavorId
            cursor.getLong(offset + 6) // GameIndex
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Natures entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setIdentifier(cursor.getString(offset + 1));
        entity.setDecreasedStatId(cursor.getLong(offset + 2));
        entity.setIncreasedStatId(cursor.getLong(offset + 3));
        entity.setHatesFlavorId(cursor.getLong(offset + 4));
        entity.setLikesFlavorId(cursor.getLong(offset + 5));
        entity.setGameIndex(cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Natures entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Natures entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getStatsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getStatsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getContestTypesDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getContestTypesDao().getAllColumns());
            builder.append(" FROM natures T");
            builder.append(" LEFT JOIN stats T0 ON T.\"decreased_stat_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN stats T1 ON T.\"increased_stat_id\"=T1.\"id\"");
            builder.append(" LEFT JOIN contest_types T2 ON T.\"hates_flavor_id\"=T2.\"id\"");
            builder.append(" LEFT JOIN contest_types T3 ON T.\"likes_flavor_id\"=T3.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Natures loadCurrentDeep(Cursor cursor, boolean lock) {
        Natures entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Stats DecreasedStat = loadCurrentOther(daoSession.getStatsDao(), cursor, offset);
         if(DecreasedStat != null) {
            entity.setDecreasedStat(DecreasedStat);
        }
        offset += daoSession.getStatsDao().getAllColumns().length;

        Stats IncreasedStat = loadCurrentOther(daoSession.getStatsDao(), cursor, offset);
         if(IncreasedStat != null) {
            entity.setIncreasedStat(IncreasedStat);
        }
        offset += daoSession.getStatsDao().getAllColumns().length;

        ContestTypes HatesFlavor = loadCurrentOther(daoSession.getContestTypesDao(), cursor, offset);
         if(HatesFlavor != null) {
            entity.setHatesFlavor(HatesFlavor);
        }
        offset += daoSession.getContestTypesDao().getAllColumns().length;

        ContestTypes LikesFlavor = loadCurrentOther(daoSession.getContestTypesDao(), cursor, offset);
         if(LikesFlavor != null) {
            entity.setLikesFlavor(LikesFlavor);
        }

        return entity;    
    }

    public Natures loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Natures> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Natures> list = new ArrayList<Natures>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Natures> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Natures> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
