package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ConquestWarriorSpecialties;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "conquest_warrior_specialties".
*/
public class ConquestWarriorSpecialtiesDao extends AbstractDao<ConquestWarriorSpecialties, Long> {

    public static final String TABLENAME = "conquest_warrior_specialties";

    /**
     * Properties of entity ConquestWarriorSpecialties.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property WarriorId = new Property(0, long.class, "WarriorId", true, "warrior_id");
        public final static Property TypeId = new Property(1, long.class, "TypeId", false, "type_id");
        public final static Property Slot = new Property(2, long.class, "Slot", false, "slot");
    };

    private DaoSession daoSession;


    public ConquestWarriorSpecialtiesDao(DaoConfig config) {
        super(config);
    }
    
    public ConquestWarriorSpecialtiesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"conquest_warrior_specialties\" (" + //
                "\"warrior_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: WarriorId
                "\"type_id\" INTEGER NOT NULL ," + // 1: TypeId
                "\"slot\" INTEGER NOT NULL );"); // 2: Slot
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_conquest_warrior_specialties_warrior_id_type_id_slot ON conquest_warrior_specialties" +
                " (\"warrior_id\",\"type_id\",\"slot\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"conquest_warrior_specialties\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConquestWarriorSpecialties entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getWarriorId());
        stmt.bindLong(2, entity.getTypeId());
        stmt.bindLong(3, entity.getSlot());
    }

    @Override
    protected void attachEntity(ConquestWarriorSpecialties entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ConquestWarriorSpecialties readEntity(Cursor cursor, int offset) {
        ConquestWarriorSpecialties entity = new ConquestWarriorSpecialties( //
            cursor.getLong(offset + 0), // WarriorId
            cursor.getLong(offset + 1), // TypeId
            cursor.getLong(offset + 2) // Slot
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConquestWarriorSpecialties entity, int offset) {
        entity.setWarriorId(cursor.getLong(offset + 0));
        entity.setTypeId(cursor.getLong(offset + 1));
        entity.setSlot(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConquestWarriorSpecialties entity, long rowId) {
        entity.setWarriorId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ConquestWarriorSpecialties entity) {
        if(entity != null) {
            return entity.getWarriorId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getConquestWarriorsDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getTypesDao().getAllColumns());
            builder.append(" FROM conquest_warrior_specialties T");
            builder.append(" LEFT JOIN conquest_warriors T0 ON T.\"warrior_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN types T1 ON T.\"type_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ConquestWarriorSpecialties loadCurrentDeep(Cursor cursor, boolean lock) {
        ConquestWarriorSpecialties entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ConquestWarriors ConquestWarriors = loadCurrentOther(daoSession.getConquestWarriorsDao(), cursor, offset);
         if(ConquestWarriors != null) {
            entity.setConquestWarriors(ConquestWarriors);
        }
        offset += daoSession.getConquestWarriorsDao().getAllColumns().length;

        Types Types = loadCurrentOther(daoSession.getTypesDao(), cursor, offset);
         if(Types != null) {
            entity.setTypes(Types);
        }

        return entity;    
    }

    public ConquestWarriorSpecialties loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ConquestWarriorSpecialties> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ConquestWarriorSpecialties> list = new ArrayList<ConquestWarriorSpecialties>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ConquestWarriorSpecialties> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ConquestWarriorSpecialties> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
