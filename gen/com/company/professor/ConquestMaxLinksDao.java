package com.company.professor;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.company.professor.ConquestMaxLinks;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "conquest_max_links".
*/
public class ConquestMaxLinksDao extends AbstractDao<ConquestMaxLinks, Long> {

    public static final String TABLENAME = "conquest_max_links";

    /**
     * Properties of entity ConquestMaxLinks.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property WarriorRankId = new Property(0, long.class, "WarriorRankId", true, "warrior_rank_id");
        public final static Property PokemonSpeciesId = new Property(1, long.class, "PokemonSpeciesId", false, "pokemon_species_id");
        public final static Property MaxLink = new Property(2, long.class, "MaxLink", false, "max_link");
    };

    private DaoSession daoSession;


    public ConquestMaxLinksDao(DaoConfig config) {
        super(config);
    }
    
    public ConquestMaxLinksDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"conquest_max_links\" (" + //
                "\"warrior_rank_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: WarriorRankId
                "\"pokemon_species_id\" INTEGER NOT NULL ," + // 1: PokemonSpeciesId
                "\"max_link\" INTEGER NOT NULL );"); // 2: MaxLink
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_conquest_max_links_warrior_rank_id_pokemon_species_id ON conquest_max_links" +
                " (\"warrior_rank_id\",\"pokemon_species_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"conquest_max_links\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConquestMaxLinks entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getWarriorRankId());
        stmt.bindLong(2, entity.getPokemonSpeciesId());
        stmt.bindLong(3, entity.getMaxLink());
    }

    @Override
    protected void attachEntity(ConquestMaxLinks entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ConquestMaxLinks readEntity(Cursor cursor, int offset) {
        ConquestMaxLinks entity = new ConquestMaxLinks( //
            cursor.getLong(offset + 0), // WarriorRankId
            cursor.getLong(offset + 1), // PokemonSpeciesId
            cursor.getLong(offset + 2) // MaxLink
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConquestMaxLinks entity, int offset) {
        entity.setWarriorRankId(cursor.getLong(offset + 0));
        entity.setPokemonSpeciesId(cursor.getLong(offset + 1));
        entity.setMaxLink(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConquestMaxLinks entity, long rowId) {
        entity.setWarriorRankId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ConquestMaxLinks entity) {
        if(entity != null) {
            return entity.getWarriorRankId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getConquestWarriorRanksDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPokemonSpeciesDao().getAllColumns());
            builder.append(" FROM conquest_max_links T");
            builder.append(" LEFT JOIN conquest_warrior_ranks T0 ON T.\"warrior_rank_id\"=T0.\"id\"");
            builder.append(" LEFT JOIN pokemon_species T1 ON T.\"pokemon_species_id\"=T1.\"id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ConquestMaxLinks loadCurrentDeep(Cursor cursor, boolean lock) {
        ConquestMaxLinks entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ConquestWarriorRanks ConquestWarriorRanks = loadCurrentOther(daoSession.getConquestWarriorRanksDao(), cursor, offset);
         if(ConquestWarriorRanks != null) {
            entity.setConquestWarriorRanks(ConquestWarriorRanks);
        }
        offset += daoSession.getConquestWarriorRanksDao().getAllColumns().length;

        PokemonSpecies PokemonSpecies = loadCurrentOther(daoSession.getPokemonSpeciesDao(), cursor, offset);
         if(PokemonSpecies != null) {
            entity.setPokemonSpecies(PokemonSpecies);
        }

        return entity;    
    }

    public ConquestMaxLinks loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ConquestMaxLinks> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ConquestMaxLinks> list = new ArrayList<ConquestMaxLinks>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ConquestMaxLinks> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ConquestMaxLinks> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
